
/*
namespace Skill_00
{
    public class Skill : SkillBase
    {
        private Skill_NonCD skill;
        private static List<Vector3> Dir = new List<Vector3>() 
        { 
            new Vector3(0,0,0),new Vector3(1,0,0),new Vector3(-1,0,0),new Vector3(0,1,0),new Vector3(0,-1,0)
        };
        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "追踪子弹";
            Description = "向前发射一簇子弹，自动瞄准敌方，倍率1";
            sprite = Tool.VarietyManager.GetSkillIcon(5, 2);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "追踪";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(1);
        }
        public override bool CanUse()
        {
            return true;
        }
        public override void UseSkill()
        {
            var data = PlayerData.GetNearestEnemy(10, true);
            Vector3 target;
            if (data == null)
            {
                if (PlayerData.FacingRight()) target = PlayerData.transform.position + Vector3.right;
                else target = PlayerData.transform.position + Vector3.left;
            }
            else
            {
                target = data.transform.position;
            }
            foreach (var i in Dir)
                Tool.BulletManager.ShootBullet(new BulletFT(PlayerData.transform.position, target, 5, 2), PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 0), 1f, null, null);
        }
        public override void Update()
        {

        }
    }
}
namespace Skill_01
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private float storeTime = 1;
        private readonly int cost = 300;
        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "束缚";
            Description = "使范围20格内的敌方角色在5秒内无法移动，耗魔300，冷却30s";
            sprite = Tool.VarietyManager.GetSkillIcon(6, 4);
            TimeNeeded = 0.1f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "控制";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
        }
        public override bool CanUse()
        {
            return storeTime > 0.999f && PlayerData.Mofa >= cost;
        }
        public override void UseSkill()
        {
            PlayerData.Mofa -= cost;
            var i = PlayerData.GetEnemyInRange(20, false);
            foreach (var p in i)
            {
                p.ApplyMotion(new Motion_01());
                Tool.BulletManager.WarnAt(p.transform.position);
            }
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 30;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost)
            {
                skill.SetAvailableTime(storeTime);
            }
            else
            {
                skill.SetAvailableTime(0);
            }
        }
    }
    public class Motion_01 : MotionBase
    {
        public override void Init()
        {
            WorkTime = 5;
        }
        public override Vector3 GetVelocity(Vector3 v)
        {
            return new Vector3(0, -5, 0);
        }
    }
}
namespace Skill_02
{
    public class Skill : SkillBase
    {
        private Skill_Storable skill;
        private float storeTime;
        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "冲刺";
            Description = "向前冲刺一段距离，冷却10s，最大储存4次";
            sprite = Tool.VarietyManager.GetSkillIcon(6, 18);
            TimeNeeded = 0.3f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "位移";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
            skill.Init(key, sprite);
            storeTime = 4;
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return storeTime > 0.999f;
        }
        public override void UseSkill()
        {
            var m = new Motion_02();
            m.SetData(PlayerData.PlayerController.FaceRight);
            ApplyMotion(m);
            PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Gongji, 0.2f, 10);
            storeTime -= 1;
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 10;
            if (storeTime > 4) storeTime = 4;
            else
            {
                skill.SetAvailableTime(storeTime);
            }
        }
    }
    public class Motion_02 : MotionBase
    {
        private int RightDir;
        public override void Init()
        {
            WorkTime = 0.3f;
        }
        public void SetData(bool fr)
        {
            RightDir = fr ? 1 : -1;
        }
        public override Vector3 GetVelocity(Vector3 v)
        {
            return new Vector3(Mathf.Max(Mathf.Abs(v.x), 50) * RightDir, 0, 0);
        }
        public override Vector3 Exit(Vector3 v)
        {
            return new Vector3();
        }
    }
}
namespace Skill_03
{
    public class Skill : SkillBase
    {
        private Skill_NonCD skill;
        private readonly int cost = 80;
        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "霰弹连发";
            Description = "向前连发多发霰弹，耗魔80";
            sprite = Tool.VarietyManager.GetSkillIcon(6, 13);
            TimeNeeded = 1f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "爆发";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
            skill.Init(key, sprite);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa > cost;
        }
        public override void UseSkill()
        {
            PlayerData.Mofa -= cost;
            Tool.BulletManager.ShootBullet(new Bullet_03() { degree = -30 }, PlayerData, PlayerData.transform.position,
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet_03() { degree = -15 }, PlayerData, PlayerData.transform.position,
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet_03() { degree = 0 }, PlayerData, PlayerData.transform.position,
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet_03() { degree = 15 }, PlayerData, PlayerData.transform.position,
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet_03() { degree = 30 }, PlayerData, PlayerData.transform.position,
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);

            TimeLineEvents e = new TimeLineEvents();
            e.Add(0.3f, () =>
            {
                Tool.BulletManager.ShootBullet(new Bullet_03() { degree = -35 }, PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
                Tool.BulletManager.ShootBullet(new Bullet_03() { degree = -20 }, PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
                Tool.BulletManager.ShootBullet(new Bullet_03() { degree = -5 }, PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
                Tool.BulletManager.ShootBullet(new Bullet_03() { degree = 10 }, PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
                Tool.BulletManager.ShootBullet(new Bullet_03() { degree = 25 }, PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
            });
            ApplyEvents(e);

            e = new TimeLineEvents();
            e.Add(0.6f, () =>
            {
                Tool.BulletManager.ShootBullet(new Bullet_03() { degree = -25 }, PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
                Tool.BulletManager.ShootBullet(new Bullet_03() { degree = -10 }, PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
                Tool.BulletManager.ShootBullet(new Bullet_03() { degree = 5 }, PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
                Tool.BulletManager.ShootBullet(new Bullet_03() { degree = 20 }, PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
                Tool.BulletManager.ShootBullet(new Bullet_03() { degree = 35 }, PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1f, null, null);
            });
            ApplyEvents(e);
        }
        public override void Update()
        {
            if (PlayerData.Mofa > cost) skill.SetAvailableTime(1);
            else skill.SetAvailableTime(0);
        }
    }
    public class Bullet_03 : BulletBase
    {
        public float degree;
        private readonly float speed = 10;
        public override void Init()
        {
            LifeTime = 0.5f;

            DamageType = new Damage_Once();


        }
        public override Vector3 GetPosition()
        {
            return new Vector3(SpawnTime * speed * Mathf.Cos(degree / 57.3f), SpawnTime * speed * Mathf.Sin(degree / 57.3f));
        }
    }
}
namespace Skill_04
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private readonly int cost = 120;
        private float storeTime = 1;
        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "绞肉机";
            Description = "召唤12颗子弹环绕自身，耗魔120";
            sprite = Tool.VarietyManager.GetSkillIcon(2, 10);
            TimeNeeded = 0.1f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "范围\n脱手";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return storeTime >= 1;
        }
        public override void UseSkill()
        {
            storeTime -= 1;

            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 0, r = 3 }, PlayerData, PlayerData.transform.position + new Vector3(6, 0, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 90, r = 3 }, PlayerData, PlayerData.transform.position + new Vector3(0, 6, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 180, r = 3 }, PlayerData, PlayerData.transform.position + new Vector3(-6, 0, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 270, r = 3 }, PlayerData, PlayerData.transform.position + new Vector3(0, -6, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 0, r = 4 }, PlayerData, PlayerData.transform.position + new Vector3(6, 0, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 90, r = 4 }, PlayerData, PlayerData.transform.position + new Vector3(0, 6, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 180, r = 4 }, PlayerData, PlayerData.transform.position + new Vector3(-6, 0, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 270, r = 4 }, PlayerData, PlayerData.transform.position + new Vector3(0, -6, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 0, r = 5 }, PlayerData, PlayerData.transform.position + new Vector3(6, 0, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 90, r = 5 }, PlayerData, PlayerData.transform.position + new Vector3(0, 6, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 180, r = 5 }, PlayerData, PlayerData.transform.position + new Vector3(-6, 0, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
            Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 270, r = 5 }, PlayerData, PlayerData.transform.position + new Vector3(0, -6, 0),
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, null, null);
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 30;
            if (storeTime > 1)
            {
                storeTime = 1;
            }
            if (PlayerData.Mofa > cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
    public class Bullet4 : BulletBase
    {
        public float r = 6;
        public float StartRate;
        private float startTime;

        private float t;
        public override void Init()
        {
            LifeTime = 20f;

            DamageType = new Damage_Time
            {
                CoolDown = 1f
            };
            MotionSpace = MoveSpace.Player;
            startTime = Time.time;
        }
        public override Vector3 GetPosition()
        {
            t = ((Time.time - startTime) * 20 * r + StartRate) / 57.3f;
            return new Vector3(Mathf.Cos(t) * r, Mathf.Sin(t) * r, 0);
        }
    }
}
namespace Skill_05
{
    public class Skill : SkillBase
    {
        private Skill_Storable skill;
        //private int cost = 0;
        private float storeTime = 2;
        private readonly float MaxStoreTime = 2;
        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "强化";
            Description = "10s内暴击率提升100%";
            sprite = Tool.VarietyManager.GetSkillIcon(5, 6);
            TimeNeeded = 0.1f;
            this.isLocalPlayer = isLocalPlayer;

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return storeTime > 0.99f;
        }
        public override void UseSkill()
        {
            storeTime -= 1;
            skill.SetAvailableTime(0);

            PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Baoji, 100, 10);
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 20;
            if (storeTime > MaxStoreTime) storeTime = MaxStoreTime;

            skill.SetAvailableTime(storeTime);
        }
    }
}

namespace Skill_10
{
    public class Skill : SkillBase
    {
        private Skill_NonCD skill;
        private Transform b;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "爆破弹";
            Description = "发射一枚子弹，造成1.2倍率的伤害，再次使用技能可引爆子弹，造成2倍率的伤害";
            sprite = Tool.VarietyManager.GetSkillIcon(1, 0);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "范围";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(1);
        }
        public override bool CanUse()
        {
            return true;
        }
        public override void UseSkill()
        {
            if (b == null)
            {
                b = Tool.BulletManager.ShootBullet(new Bullet1(), PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 0), 1.2f, null, null).transform;
            }
            else
            {
                Vector3 v = b.position;
                Tool.Destroy(b);
                Tool.BulletManager.ShootBullet(new Bullet2(), PlayerData, v,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 2f, null, null);
            }
        }
        public override void Update()
        {

        }
    }
    public class Bullet1 : BulletBase
    {
        public override void Init()
        {
            LifeTime = 3f;

            DamageType = new Damage_Once();
        }
        public override Vector3 GetPosition()
        {
            return new Vector3(10 * SpawnTime, 0, 0);
        }
    }
    public class Bullet2 : BulletBase
    {
        public override void Init()
        {
            LifeTime = 1f;

            DamageType = new Damage_Once();
        }
        public override Vector3 GetPosition()
        {
            return new Vector3(0, 0, 0);
        }
    }
}
namespace Skill_11
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private readonly int cost = 70;
        private float storeTime = 1;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "强化";
            Description = "大幅提高速度，防御和攻击，持续10秒，耗魔200，冷却30秒";
            sprite = Tool.VarietyManager.GetSkillIcon(1, 3);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "增益";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime >= 0.999f;
        }
        public override void UseSkill()
        {
            PlayerData.Mofa -= cost;
            storeTime = 0;

            PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Fangyu, 1.2f, 10);
            PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Gongji, 1.2f, 10);
            PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Jixing, 0.4f, 10);
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 30f;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
}
namespace Skill_12
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private readonly int cost = 150;
        private float storeTime = 1;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "闪电风暴";
            Description = "召唤巨大的闪电球环绕自身，倍率8，耗魔150，冷却30秒";
            sprite = Tool.VarietyManager.GetSkillIcon(5, 9);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "大范围";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime >= 0.999f;
        }
        public override void UseSkill()
        {
            PlayerData.Mofa -= cost;
            storeTime = 0;
            Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 8), 8, null, null);

        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 30f;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
    public class Bullet : BulletBase
    {
        public override void Init()
        {
            LifeTime = 2f;

            DamageType = new Damage_Once();
        }
        public override Vector3 GetPosition()
        {
            return new Vector3(15 * Mathf.Cos(180 * SpawnTime / 57.3f), 15 * Mathf.Sin(180 * SpawnTime / 57.3f), 0);
        }
    }
}
namespace Skill_13
{
    public class Skill : SkillBase
    {
        private Skill_Storable skill;
        private float storeTime = 3;
        private readonly int cost = 60;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "牵引";
            Description = "中幅降低5格范围内敌人的移速和跳跃，持续5秒，可储存3次，耗魔60";
            sprite = Tool.VarietyManager.GetSkillIcon(2, 11);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "控制";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime > 0.999f;
        }
        public override void UseSkill()
        {
            var i = PlayerData.GetNearestEnemy(10, true);
            if (i == null) return;

            storeTime -= 1;
            if (skill != null) skill.SetAvailableTime(storeTime);

            i.GetEffectable().AddEffectLocal(Effectable.Effects.Jixing, -0.4f, 5);
            i.GetEffectable().AddEffectLocal(Effectable.Effects.Tengkong, -0.2f, 5);
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 5;
            if (storeTime > 3) storeTime = 3;
            if (PlayerData.Mofa < cost)
            {
                skill.SetAvailableTime(0);
            }
            else
            {
                skill.SetAvailableTime(storeTime);
            }
        }
    }
}
namespace Skill_14
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private readonly int cost = 70;
        private float storeTime = 1;
        private EffectCollection effect;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "空间爆破";
            Description = "在全体敌人位置召唤巨大爆炸，倍率5，命中后降低敌人20%攻击，持续40秒，并召唤巨大闪电球砸下，倍率7，耗魔200，冷却60秒";
            sprite = Tool.VarietyManager.GetSkillIcon(4, 17);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "自瞄\n爆发\n减益";

            effect = new EffectCollection();
            effect.AddEffect(EffectController.Effects.Gongji, -0.2f, 20);

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime >= 0.999f;
        }
        public override void UseSkill()
        {
            PlayerData.Mofa -= cost;
            storeTime = 0;

            var i = PlayerData.GetEnemyInRange(999999, false);
            foreach (var p in i)
            {
                Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, p.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 5f, effect, null);
                Tool.BulletManager.ShootBullet(new BulletFT(p.transform.position+Vector3.up*50,p.transform.position,5,12), PlayerData, p.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 7f, null, null);
            }
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 60f;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
    public class Bullet : BulletBase
    {
        public override void Init()
        {
            LifeTime = 1f;

            DamageType = new Damage_Once();
        }
        public override Vector3 GetPosition()
        {
            return new Vector3(0, 0, 0);
        }
    }
}
namespace Skill_15
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private int cost = 100;
        private float storeTime = 1;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "御风";
            Description = "5秒内大幅提升移速，跳跃，且可无限连跳，耗魔100，冷却20秒";
            sprite = Tool.VarietyManager.GetSkillIcon(9, 59);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "增益";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime >= 0.999f;
        }
        public override void UseSkill()
        {
            PlayerData.Mofa -= cost;
            storeTime = 0;
            PlayerData.GetEffectable().AddEffectLocal(Effectable.Effects.Jixing, 0.2f, 5);
            PlayerData.GetEffectable().AddEffectLocal(Effectable.Effects.Tengkong, 0.2f, 5);
            PlayerData.GetEffectable().AddEffectLocal(Effectable.Effects.Liantiao, 100, 5);
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 20f;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
}

namespace Skill_20
{
    public class Skill : SkillBase
    {
        private Skill_NonCD skill;
        private readonly int cost = 30;


        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "喷射";
            Description = "向前方短距离喷发子弹，倍率0.8，耗魔30";
            sprite = Tool.VarietyManager.GetSkillIcon(2, 2);
            TimeNeeded = 0.3f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "范围";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
            skill.Init(key, sprite);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost;
        }
        public override void UseSkill()
        {
            PlayerData.Mofa -= cost;
            Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, PlayerData.transform.position,
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 12), 0.6f, null, null);
        }
        public override void Update()
        {
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
            else skill.SetAvailableTime(0);
        }
    }
    public class Bullet : BulletBase
    {
        public override void Init()
        {
            LifeTime = 0.8f;

            DamageType = new Damage_Once();
        }
        public override Vector3 GetPosition()
        {
            return new Vector3(0, 0, 0);
        }
    }
}
namespace Skill_21
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private readonly int cost = 60;
        private float storeTime = 1;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "连发";
            Description = "向20格内最近的敌人连续射出多枚子弹，倍率0.6，耗魔60，冷却20秒";
            sprite = Tool.VarietyManager.GetSkillIcon(3, 6);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "自瞄";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime >= 0.999f&& PlayerData.GetNearestEnemy(20, true)!=null;
        }
        public override void UseSkill()
        {
            var p = PlayerData.GetNearestEnemy(20, true);
            if (p == null) return;

            PlayerData.Mofa -= cost;
            storeTime = 0;

            TimeLineEvents events = new TimeLineEvents();
            for (int i = 0; i < 6; i++)
            {
                events.Add(i * 0.4f, () =>
                {
                    Tool.BulletManager.ShootBullet(new BulletFT(PlayerData.transform.position,p.transform.position,15,2), PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 13), 0.6f, null, null);
                });
            }
            ApplyEvents(events);
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 20f;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
}
namespace Skill_22
{
    public class Skill : SkillBase
    {
        private Skill_Storable skill;
        private float storeTime;
        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "冲刺";
            Description = "向前冲刺一段距离，并在10s内提升40%速度和20%跳跃，冷却20s，最大储存2次";
            sprite = Tool.VarietyManager.GetSkillIcon(0, 16);
            TimeNeeded = 0.1f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "位移\n增益";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
            skill.Init(key, sprite);
            storeTime = 2;
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return storeTime > 0.999f;
        }
        public override void UseSkill()
        {
            var m = new Motion();
            m.SetData(PlayerData.PlayerController.FaceRight);
            ApplyMotion(m);
            PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Jixing, 0.4f, 10);
            PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Tengkong, 0.2f, 10);
            storeTime -= 1;
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 20;
            if (storeTime > 2) storeTime = 2;
            else
            {
                skill.SetAvailableTime(storeTime);
            }
        }
    }
    public class Motion : MotionBase
    {
        private int RightDir;
        public override void Init()
        {
            WorkTime = 0.3f;
        }
        public void SetData(bool fr)
        {
            RightDir = fr ? 1 : -1;
        }
        public override Vector3 GetVelocity(Vector3 v)
        {
            return new Vector3(Mathf.Max(Mathf.Abs(v.x), 50) * RightDir, 0, 0);
        }
        public override Vector3 Exit(Vector3 v)
        {
            return new Vector3();
        }
    }
}
namespace Skill_23
{
    public class Skill : SkillBase
    {
        private Skill_Storable skill;
        private float storeTime;
        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "爆破疾行";
            Description = "原地引发爆炸，向前冲刺一段距离，冲刺结束后造成爆炸，倍率1.6，冷却20s，最大储存2次";
            sprite = Tool.VarietyManager.GetSkillIcon(0, 10);
            TimeNeeded = 0.1f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "位移\n范围";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
            skill.Init(key, sprite);
            storeTime = 2;
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return storeTime > 0.999f;
        }
        public override void UseSkill()
        {
            var m = new Motion();
            storeTime -= 1;
            m.SetData(PlayerData.PlayerController.FaceRight);
            ApplyMotion(m);

            Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 1.6f, null, null);

            TimeLineEvents events = new TimeLineEvents();
            events.Add(0.3f, () => 
            {
                Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 1.6f, null, null);
            });
            ApplyEvents(events);
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 20;
            if (storeTime > 2) storeTime = 2;
            else
            {
                skill.SetAvailableTime(storeTime);
            }
        }
    }

    public class Bullet : BulletBase
    {
        public override void Init()
        {
            LifeTime = 5f;

            DamageType = new Damage_Once();
        }
        public override Vector3 GetPosition()
        {
            return new Vector3(0, 0, 0);
        }
    }
    public class Motion : MotionBase
    {
        private int RightDir;
        public override void Init()
        {
            WorkTime = 0.3f;
        }
        public void SetData(bool fr)
        {
            RightDir = fr ? 1 : -1;
        }
        public override Vector3 GetVelocity(Vector3 v)
        {
            return new Vector3(Mathf.Max(Mathf.Abs(v.x), 50) * RightDir, 0, 0);
        }
        public override Vector3 Exit(Vector3 v)
        {
            return new Vector3();
        }
    }
}
namespace Skill_24
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private readonly int cost = 400;
        private float storeTime = 1;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "滚石";
            Description = "丢出三枚巨大的子弹，倍率10，耗魔400，冷却60秒";
            sprite = Tool.VarietyManager.GetSkillIcon(3, 2);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "范围\n爆发";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime >= 0.999f;
        }
        public override void UseSkill()
        {
            PlayerData.Mofa -= cost;
            storeTime = 0;

            TimeLineEvents e = new TimeLineEvents();
            for (int i = 0; i < 3; i++)
            {
                Tool.BulletManager.ShootBullet(new Bullet() { degree=75-15*i}, PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 14), 10f, null, null);
            }
            ApplyEvents(e);


        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 60f;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
    public class Bullet : BulletBase
    {
        public float degree;
        private readonly float Speed = 10;
        public override void Init()
        {
            LifeTime = 2f;

            DamageType = new Damage_Once();
        }
        public override Vector3 GetPosition()
        {
            return new Vector3(Speed * SpawnTime * Mathf.Cos(degree / 57.3f), Speed * Mathf.Sin(degree / 57.3f) * SpawnTime - 5 * SpawnTime * SpawnTime, 0);
        }
    }
}
namespace Skill_25
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private readonly int cost = 100;
        private float storeTime = 1;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "隔空传送";
            Description = "瞬移到附近的一名敌人位置，5秒内攻击提升80%，倍率0.4，耗魔100，冷却40秒";
            sprite = Tool.VarietyManager.GetSkillIcon(6, 0);
            TimeNeeded = 0.1f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "位移";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime >= 0.999f;
        }
        public override void UseSkill()
        {
            var p = PlayerData.GetNearestEnemy(40, false); ;

            if (p == null) return;
            PlayerData.transform.position = p.transform.position;

            PlayerData.GetEffectable().AddEffectLocal(Effectable.Effects.Gongji, 0.8f, 5);

            PlayerData.Mofa -= cost;
            storeTime = 0;

        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 40f;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
}

namespace Skill_30
{
    public class Skill : SkillBase
    {
        private Skill_NonCD skill;
        private readonly int cost = 40;
        private EffectCollection effect;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "爆破";
            Description = "在自身位置造成爆炸，敌人被命中后防御降低5%，倍率0.6，耗魔40";
            sprite = Tool.VarietyManager.GetSkillIcon(6, 8);
            TimeNeeded = 0.5f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "范围\n减益";

            effect = new EffectCollection();
            effect.AddEffect(EffectController.Effects.Fangyu, -0.05f, 20);

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
            skill.Init(key, sprite);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost;
        }
        public override void UseSkill()
        {
            PlayerData.Mofa -= cost;
            Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, PlayerData.transform.position,
                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 0.6f, effect, null);
        }
        public override void Update()
        {
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
            else skill.SetAvailableTime(0);
        }
    }
    public class Bullet : BulletBase
    {
        public override void Init()
        {
            LifeTime = 1f;

            DamageType = new Damage_Once();
        }
        public override Vector3 GetPosition()
        {
            return new Vector3(0, 0, 0);
        }
    }
}
namespace Skill_31
{
    public class Skill : SkillBase
    {
        private Skill_Storable skill;
        private float storeTime = 3;
        private readonly int cost = 200;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "生命汲取";
            Description = "治疗自身，吸收10格范围内的部分生命，可储存3次，耗魔200";
            sprite = Tool.VarietyManager.GetSkillIcon(4, 14);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "治疗\n自瞄";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime > 0.999f;
        }
        public override void UseSkill()
        {
            storeTime -= 1;
            if(isLocalPlayer)skill.SetAvailableTime(storeTime);

            PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Shengming, 0.01f, 1);
            foreach (var i in PlayerData.GetEnemyInRange(10, false))
            {
                PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Shengming, 0.002f, 1);
                Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, i.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 5), 0.6f, null, null);
            }
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 5;
            if (storeTime > 3) storeTime = 3;
            if (PlayerData.Mofa < cost)
            {
                skill.SetAvailableTime(0);
            }
            else
            {
                skill.SetAvailableTime(storeTime);
            }
        }
    }
    public class Bullet : BulletBase
    {
        public override void Init()
        {
            LifeTime = 1f;

            DamageType = new Damage_Once();
        }
        public override Vector3 GetPosition()
        {
            return new Vector3(0, 0, 0);
        }
    }
}
namespace Skill_32
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private readonly int cost = 70;
        private float storeTime = 1;
        private EffectCollection effect;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "火墙";
            Description = "召唤多个火墙，持续20s，击中敌人后降低敌人攻击和魔法，倍率0.2，耗魔150，冷却40秒";
            sprite = Tool.VarietyManager.GetSkillIcon(6, 10);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "控制\n减益";

            effect = new EffectCollection();
            effect.AddEffect(EffectController.Effects.Gongji, -0.1f, 10);
            effect.AddEffect(EffectController.Effects.Mofa, -100, 1);

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime >= 0.999f;
        }
        public override void UseSkill()
        {
            PlayerData.Mofa -= cost;
            storeTime = 0;

            for(int i = -2; i < 3; i++)
            {
                Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, PlayerData.transform.position+new Vector3(i*10,0,0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 9), 0.2f, effect, null);
            }
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 20f;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
    public class Bullet : BulletBase
    {
        public override void Init()
        {
            LifeTime = 20f;

            DamageType = new Damage_Time()
            {
                CoolDown=0.2f
            };
        }
        public override Vector3 GetPosition()
        {
            return new Vector3(0, 0, 0);
        }
        public override float GetRotation()
        {
            return 90;
        }
    }
}
namespace Skill_33
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private readonly int cost = 200;
        private float storeTime = 1;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "闪电牢笼";
            Description = "在最近的敌人位置召唤三个闪电球，靠近敌人后爆炸，耗魔200，冷却40秒";
            sprite = Tool.VarietyManager.GetSkillIcon(6, 14);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "自瞄\n爆发";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime >= 0.999f;
        }
        public override void UseSkill()
        {
            var p = PlayerData.GetNearestEnemy(99999, false);
            if (p == null) return;
            PlayerData.Mofa -= cost;
            storeTime = 0;
            for(int i = 0; i < 3; i++)
            {
                Tool.BulletManager.ShootBullet(new Bullet() { StartDegree=120*i}, PlayerData, 
                    p.transform.position,Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 7), 1.2f, null, null);
            }
            TimeLineEvents e = new TimeLineEvents();
            e.Add(1, () =>
            {
                Tool.BulletManager.ShootBullet(new Bullet2(), PlayerData,
                    p.transform.position, Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 4f, null, null);
             });
            ApplyEvents(e);
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 40f;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
    public class Bullet : BulletBase
    {
        public float StartDegree;
        private float degree;
        private float magnitude;
        public override void Init()
        {
            LifeTime = 1f;

            DamageType = new Damage_Once();
        }
        public override Vector3 GetPosition()
        {
            magnitude = 10 - SpawnTime*10;
            degree = StartDegree + SpawnTime * 540;
            return new Vector3(magnitude*Mathf.Cos(degree/57.3f),magnitude*Mathf.Sin(degree/57.3f), 0);
        }
    }
    public class Bullet2 : BulletBase
    {
        public override void Init()
        {
            LifeTime = 1f;

            DamageType = new Damage_Once();
        }
        public override Vector3 GetPosition()
        {
            return new Vector3(0, 0, 0);
        }
    }
}
namespace Skill_34
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private readonly int cost = 500;
        private float storeTime = 1;
        private HitEventBase hitEvent;
        private EffectCollection effects;
        private readonly List<float> SpawnTime = new List<float>() 
        { 0,0.2f,0.3f,0.6f,0.7f,1f,1.1f,1.4f,1.5f,2f};
        private readonly List<Vector3> Offset = new List<Vector3>()
        {
            new Vector3(-20,20),new Vector3(14,16),new Vector3(4, 25),new Vector3(-10, 14),new Vector3(22, 15),
            new Vector3(-2,28),new Vector3(17, 18),new Vector3(-12, 15),new Vector3(20, 19),new Vector3(0, 30)
        };

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "天坠列箭";
            Description = "召唤多发子弹锁定30格内最近敌人，倍率3，命中敌人会提升自身攻击，耗魔500，冷却60秒";
            sprite = Tool.VarietyManager.GetSkillIcon(0, 17);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "自瞄\n爆发\n增益";

            effects = new EffectCollection();
            effects.AddEffect(EffectController.Effects.Gongji, 0.4f, 20);
            hitEvent= new HitEvent() { effect = effects };

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime >= 0.999f;
        }
        public override void UseSkill()
        {
            var p = PlayerData.GetNearestEnemy(30, false);
            if(p == null) return;
            PlayerData.Mofa -= cost;
            storeTime = 0;

            TimeLineEvents e = new TimeLineEvents();
            for(int i = 0; i < Offset.Count; i++)
            {
                int j = i;
                e.Add(SpawnTime[i]*3, () =>
                {
                    Tool.BulletManager.ShootBullet(new Bullet() { Target=p.transform.position}, PlayerData, 
                        PlayerData.transform.position + Offset[j],
                            Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 14), 3, null, hitEvent);
                });
            }
            ApplyEvents(e);

        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 60f;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
    public class Bullet : BulletBase
    {
        public Vector3 Target;

        private Vector3 StartPosition;
        private Vector3 DirASpeed;

        public override void Init()
        {
            LifeTime = 8f;

            DamageType = new Damage_Once();
            MotionSpace = MoveSpace.World;

            StartPosition = Bullet.transform.position;
            DirASpeed = (Target - StartPosition).normalized * 25;
        }
        public override Vector3 GetPosition()
        {
            return StartPosition + SpawnTime * DirASpeed;
        }
        public override Vector3 GetPositionL()
        {
            return GetPosition();
        }
    }
    public class HitEvent:HitEventBase
    {
        public EffectCollection effect;
        public override void OnHit(Target attacker, Target defenser, BulletController bullet)
        {
            AddEffect(attacker.GetEffectable(), effect);
        }
    }
}
namespace Skill_35
{
    public class Skill : SkillBase
    {
        private Skill_CD skill;
        private readonly int cost = 150;
        private float storeTime = 1;

        public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
        {
            PlayerData = data;

            Name = "腾跃";
            Description = "疾行+30%，腾空+20%，持续10s，耗魔150，冷却13s";
            sprite = Tool.VarietyManager.GetSkillIcon(2, 1);
            TimeNeeded = 0.2f;
            this.isLocalPlayer = isLocalPlayer;

            Tag = "增益";

            if (data == null) return;
            if (!isLocalPlayer) return;
            skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
            skill.Init(key, sprite);
            skill.SetAvailableTime(storeTime);
        }
        public override bool CanUse()
        {
            return PlayerData.Mofa >= cost && storeTime >= 0.999f;
        }
        public override void UseSkill()
        {
            PlayerData.Mofa -= cost;
            storeTime = 0;
            PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Jixing, 0.3f, 10);
            PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Tengkong, 0.2f, 10);
        }
        public override void Update()
        {
            storeTime += Time.deltaTime / 13f;
            if (storeTime > 1) storeTime = 1;
            if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
            else skill.SetAvailableTime(0);
        }
    }
}*/

/*
 namespace Skill1
    {
        public class Skill1 : SkillBase
        {
            private Skill_Storable skill;
            private float storeTime = 3;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "空中跳跃";
                Description = "在空中额外跳跃一次";
                sprite = Tool.VarietyManager.GetSkillIcon(0, 6);
                TimeNeeded = 0.1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.PlayerController.Motion == null&&storeTime>1;
            }
            public override void UseSkill()
            {
                ApplyMotion(new Motion1());
                storeTime -= 1;
                skill?.SetAvailableTime(storeTime);
            }
            public override void Update()
            {
                if(storeTime<3)storeTime += Time.deltaTime / 5;
                if (storeTime > 3) storeTime = 3;
                skill?.SetAvailableTime(storeTime);
            }
        }

        public class Motion1 : MotionBase
        {
            public override void Init()
            {
                WorkTime = 0.5f;
            }
            public override Vector3 GetVelocity(Vector3 v)
            {
                return v;
            }
            public override Vector3 Entry(Vector3 v)
            {
                return new Vector3(v.x, Mathf.Max(v.y, 15), v.z);
            }
        }
    }
    namespace Skill2
    {
        public class Skill2 : SkillBase
        {
            private Skill_CD skill;
            private float storeTime;
            private int cost = 20;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "冲刺";
                Description = "向前冲刺一段距离，并10s内提高20%的攻击，耗魔20";
                sprite = Tool.VarietyManager.GetSkillIcon(0, 3);
                TimeNeeded = 0.3f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return storeTime > 0.999f && PlayerData.Mofa >= cost&&PlayerData.PlayerController.Motion==null;
            }
            public override void UseSkill()
            {
                var m = new Motion2();
                m.SetData(PlayerData.PlayerController.FaceRight);
                ApplyMotion(m);
                PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Gongji, 0.2f, 10);
                PlayerData.Mofa -= cost;
                storeTime -= 1;
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 2;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa < cost && PlayerData.PlayerController.Motion == null) skill.SetAvailableTime(0);
                else
                {
                    skill.SetAvailableTime(storeTime);
                }
            }
        }
        public class Motion2 : MotionBase
        {
            private int RightDir;
            public override void Init()
            {
                WorkTime = 0.3f;
            }
            public void SetData(bool fr)
            {
                RightDir = fr ? 1 : -1;
            }
            public override Vector3 GetVelocity(Vector3 v)
            {
                return new Vector3(Mathf.Max(Mathf.Abs(v.x), 30) * RightDir, 0, 0);
            }
            public override Vector3 Exit(Vector3 v)
            {
                return new Vector3();
            }
        }
    }
    namespace Skill3
    {
        public class Skill3 : SkillBase
        {
            private Skill_CD skill;
            private float storeTime = 1;
            private int cost = 100;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "死亡火墙";
                Description = "周围召唤多颗子弹";
                sprite = Tool.VarietyManager.GetSkillIcon(6, 11);
                TimeNeeded = 0.3f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "范围输出";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost&&storeTime>0.99f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;

                TimeLineEvents e = new TimeLineEvents();
                Vector3 start = PlayerData.transform.position;
                e.Add(0.1f, () => {
                    Tool.BulletManager.ShootBullet(new Bullet3(), PlayerData, start + new Vector3(1f, 0, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2.4f, null, null);
                });
                e.Add(0.1f, () => {
                    Tool.BulletManager.ShootBullet(new Bullet3(), PlayerData, start + new Vector3(-1f, 0, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2.4f, null, null);
                });
                e.Add(0.2f, () => {
                    Tool.BulletManager.ShootBullet(new Bullet3(), PlayerData, start + new Vector3(2f, 0, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2.4f, null, null);
                });
                e.Add(0.2f, () => {
                    Tool.BulletManager.ShootBullet(new Bullet3(), PlayerData, start + new Vector3(-2f, 0, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2.4f, null, null);
                });
                e.Add(0.3f, () => {
                    Tool.BulletManager.ShootBullet(new Bullet3(), PlayerData, start + new Vector3(3f, 0, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2.4f, null, null);
                });
                e.Add(0.3f, () => {
                    Tool.BulletManager.ShootBullet(new Bullet3(), PlayerData, start + new Vector3(-3f, 0, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2.4f, null, null);
                });
                e.Add(0.4f, () => {
                    Tool.BulletManager.ShootBullet(new Bullet3(), PlayerData, start + new Vector3(4f, 0, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2.4f, null, null);
                });
                e.Add(0.4f, () => {
                    Tool.BulletManager.ShootBullet(new Bullet3(), PlayerData, start + new Vector3(-4f, 0, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2.4f, null, null);
                });
                e.Add(0.5f, () => {
                    Tool.BulletManager.ShootBullet(new Bullet3(), PlayerData, start + new Vector3(5f, 0, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2.4f, null, null);
                });
                e.Add(0.5f, () => {
                    Tool.BulletManager.ShootBullet(new Bullet3(), PlayerData, start + new Vector3(-5f, 0, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2.4f, null, null);
                });
                ApplyEvents(e);
            }
            public override void Update()
            {
                if (storeTime < 1) storeTime += Time.deltaTime / 5;
                if(storeTime>1)storeTime = 1;

                if (PlayerData.Mofa<cost) skill.SetAvailableTime(0);
                else skill.SetAvailableTime(storeTime);
            }
        }
        public class Bullet3 : BulletBase
        {
            private int r;
            public override void Init()
            {
                LifeTime = 0.5f;

                DamageType = new Damage_Once();

                if (owner.transform.position.x < Bullet.transform.position.x) r = 1;
                else r = -1;
            }
            public override Vector3 GetPosition()
            {
                return new Vector3(SpawnTime * 3 * r, SpawnTime * 10, 0);
            }
            public override Vector3 GetPositionL()
            {
                return GetPosition();
            }
        }
    }
    namespace Skill4
    {
        public class Skill4 : SkillBase
        {
            private Skill_Storable skill;
            //private int cost = 60;
            private float storeTime=2;
            private int MaxStorableTime = 2;

            private EffectCollection effects;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "大风车";
                Description = "召唤四颗子弹环绕自身，击中敌人时降低其减伤";
                sprite = Tool.VarietyManager.GetSkillIcon(4, 19);
                TimeNeeded = 0.3f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "脱手输出\n增伤";

                effects = new EffectCollection();
                effects.AddEffect(EffectController.Effects.Jianshang, -10, 5);

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return storeTime>=1;
            }
            public override void UseSkill()
            {
                storeTime -= 1;

                Tool.BulletManager.ShootBullet(new Bullet4() {StartRate=0 }, PlayerData, PlayerData.transform.position+new Vector3(6,0,0),
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, effects, null);
                Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 90 }, PlayerData, PlayerData.transform.position + new Vector3(0, 6, 0),
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, effects, null);
                Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 180 }, PlayerData, PlayerData.transform.position + new Vector3(-6, 0, 0),
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, effects, null);
                Tool.BulletManager.ShootBullet(new Bullet4() { StartRate = 270 }, PlayerData, PlayerData.transform.position + new Vector3(0, -6, 0),
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.5f, effects, null);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 30;
                if (storeTime > MaxStorableTime) storeTime = MaxStorableTime;

                skill.SetAvailableTime(storeTime);
            }
        }
        public class Bullet4 : BulletBase
        {
            private float r = 6;
            public float StartRate;
            private float startTime;

            private float t;
            public override void Init()
            {
                LifeTime = 20f;

                DamageType = new Damage_Time
                {
                    CoolDown = 1f
                };
                MotionSpace = MoveSpace.Player;
                startTime= Time.time;
            }
            public override Vector3 GetPosition()
            {
                t = ((Time.time - startTime) * 90 + StartRate)/57.3f;
                return new Vector3(Mathf.Cos(t) * r, Mathf.Sin(t) * r, 0);
            }
        }
    }
    namespace Skill5
    {
        public class Skill5 : SkillBase
        {
            private Skill_CD skill;
            //private int cost = 0;
            private float storeTime=1;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "加速";
                Description = "10s内速度提升60%";
                sprite = Tool.VarietyManager.GetSkillIcon(1, 4);
                TimeNeeded = 0.1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return storeTime > 0.99f;
            }
            public override void UseSkill()
            {
                storeTime = 0;
                if(skill)skill.SetAvailableTime(0);

                PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Jixing, 0.6f, 10);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20;
                if (storeTime > 1) storeTime = 1;

                skill.SetAvailableTime(storeTime);
            }
        }
    }
    namespace Skill6
    {
        public class Skill6 : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "自瞄霰弹";
                Description = "向周围发射一圈子弹，并自动锁定15格内最近的敌人，倍率1.4，耗魔100";
                sprite = Tool.VarietyManager.GetSkillIcon(4, 6);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "爆发";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost&&storeTime>0.99f;
            }
            public override void UseSkill()
            {
                storeTime = 0;
                PlayerData.Mofa -= cost;
                var t = PlayerData.GetNearestEnemy(15, false);
                if (t == null) t = PlayerData;
                for(int i = 0; i < 8; i++)
                {
                    Tool.BulletManager.ShootBullet(new Bullet6() { degree = i*45, target = t }, PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.4f, null, null);
                }
                
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost)
                {
                    skill.SetAvailableTime(storeTime);
                }
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet6 : BulletBase
        {
            public float degree;
            public Target target;
            private Vector3 Dir;
            private Vector3 s;
            private Vector3 t;
            public override void Init()
            {
                LifeTime = 3;
                DamageType = new Damage_Time() { CoolDown=0.3f};
                t = new Vector3(Mathf.Cos(degree / 57.3f) * 5, Mathf.Sin(degree / 57.3f) * 5, 0);
                s = Bullet.transform.position;
                t += s;
                MotionSpace = MoveSpace.World;
            }
            public override Vector3 GetPosition()
            {
                if (SpawnTime < 1)
                {
                    float m = (SpawnTime - 1) * (SpawnTime - 1);
                    Dir = (target.transform.position - Bullet.transform.position).normalized * 10;
                    return s * m + t * (1 - m);
                }
                else
                {
                    return t + Dir * (SpawnTime - 1);
                }
            }
            public override Vector3 GetPositionL()
            {
                return GetPosition();
            }
        }
    }
    namespace Skill7
    {
        public class Skill7 : SkillBase
        {
            private Skill_CD skill;
            private int cost = 70;
            private float storeTime=1;
            private EffectCollection effects;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "减速弹";
                Description = "发射一发子弹，命中敌人时使其减速50%，持续10秒，倍率0.4，耗魔70，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(6, 27);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "控制";

                effects = new EffectCollection();
                effects.AddEffect(EffectController.Effects.Jixing, -0.5f, 10);

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost&&storeTime>=0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime =0;
                Tool.BulletManager.ShootBullet(new Bullet7(), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.4f, effects, null);

            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }

        public class Bullet7 : BulletBase
        {
            public override void Init()
            {
                LifeTime = 5f;

                DamageType = new Damage_Once();
            }
            public override Vector3 GetPosition()
            {
                return new Vector3(SpawnTime * 10, 0, 0);
            }
        }
    }
    namespace Skill8
    {
        public class Skill8 : SkillBase
        {
            private Skill_CD skill;
            private int cost = 200;
            private float storeTime = 1;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "恐吓";
                Description = "恐吓周围敌人，使其远离自身，倍率3.2，冷却10s，耗魔200";
                sprite = Tool.VarietyManager.GetSkillIcon(6, 2);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "控制";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                Tool.BulletManager.ShootBullet(new Bullet8(), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 3.2f, null, new HitEvent8());

            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 10f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet8 : BulletBase
        {
            public override void Init()
            {
                LifeTime = 2f;

                DamageType = new Damage_Once();
            }
            public override Vector3 GetPosition()
            {
                return new Vector3(0, 0, 0);
            }
        }
        public class HitEvent8 : HitEventBase
        {
            public override void OnHit(Target attacker, Target defenser, BulletController bullet)
            {
                if (defenser == null) return;
                var m = new Motion8() { FaceRight = (defenser.transform.position.x > attacker.transform.position.x) ? 1 : -1 };
                m.Init();
                defenser.ApplyMotion(m);
            }
        }
        public class Motion8 : MotionBase
        {
            public int FaceRight;
            public override void Init()
            {
                WorkTime = 5;
            }
            public override Vector3 GetVelocity(Vector3 v)
            {
                return new Vector3(FaceRight * 3, v.y, 0);
            }
        }
    }
    namespace Skill9
    {
        public class Skill9 : SkillBase
        {
            private Skill_CD skill;
            private readonly int cost = 60;
            private float storeTime = 1;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "爆破";
                Description = "向上跃起，并在原地释放三次爆炸，倍率6，耗魔120，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(4, 13);
                TimeNeeded = 0.2f;

                Tag = "位移\n爆发";


                this.isLocalPlayer = isLocalPlayer;
                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;

                var p = PlayerData.transform.position;
                var e = new TimeLineEvents();
                e.Add(0.2f, () => 
                {
                    Tool.BulletManager.ShootBullet(new Bullet9(), PlayerData, new Vector3(1, 0, 0) + p,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 5), 6, null, null);
                });
                e.Add(0.3f, () =>
                {
                    Tool.BulletManager.ShootBullet(new Bullet9(), PlayerData, new Vector3(0, 0, 0) + p,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 5), 6, null, null);
                });
                e.Add(0.4f, () =>
                {
                    Tool.BulletManager.ShootBullet(new Bullet9(), PlayerData, new Vector3(-1, 0, 0) + p,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 5), 6, null, null);
                });
                ApplyEvents(e);

                ApplyMotion(new Motion9());
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 10f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }

        public class Bullet9 : BulletBase
        {
            public override void Init()
            {
                LifeTime = 0.6f;

                DamageType = new Damage_Once();
            }
            public override Vector3 GetPosition()
            {
                return new Vector3(0, 0, 0);
            }
        }
        public class Motion9 : MotionBase
        {
            public override void Init()
            {
                WorkTime = 0.2f;
            }
            public override Vector3 GetVelocity(Vector3 v)
            {
                return v;
            }
            public override Vector3 Entry(Vector3 v)
            {
                return new Vector3(v.x, Mathf.Max(v.y, 15), v.z);
            }
        }
    }
    namespace Skill10
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 70;
            private float storeTime = 1;
            private List<float> degrees = new List<float>
            {
                10,0,40,160,90,120,140,310,330,290,
                190,180,220,250,240,60,70,130,270,285
            };
            private List<float> speed = new List<float>
            {
                9,6,12,15,7.5f,11,13,12,10,10.5f,
                8,8.5f,9.5f,10,10.5f,14,15,11,12,12.5f
            };

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "拉坨大的";
                Description = "向四周喷射大量子弹，倍率0.2，耗魔70，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(2, 8);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "输出\n大范围";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                for(int i = 0; i < 20;i++)
                Tool.BulletManager.ShootBullet(new Bullet() { degree = degrees[i], speed = speed[i],LifeTime=1+i*0.05f }, PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.2f, null, null);

            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : BulletBase
        {
            public float degree;
            public float speed;
            public override void Init()
            {
                DamageType = new Damage_Once();
            }
            public override Vector3 GetPosition()
            {
                return new Vector3(Mathf.Cos(degree/57.3f)*speed * SpawnTime, Mathf.Sin(degree / 57.3f) * speed * SpawnTime, 0);
            }
        }
    }
    namespace Skill11
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 5;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "极限手速";
                Description = "向前方扇形范围攻击，倍率0.4，耗魔5";
                sprite = Tool.VarietyManager.GetSkillIcon(2, 4);
                TimeNeeded = 0.01f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "近身爆发";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 12), 0.4f, null, null);
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : BulletBase
        {
            public override void Init()
            {
                LifeTime = 0.5f;

                DamageType = new Damage_Once();
            }
            public override Vector3 GetPosition()
            {
                return new Vector3(0.1f*SpawnTime, 0, 0);
            }
        }
    }
    namespace Skill12
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 70;
            private float storeTime = 1;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "喷发";
                Description = "在周围召唤多个火柱，倍率1.2，耗魔120，冷却5秒";
                sprite = Tool.VarietyManager.GetSkillIcon(4, 9);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "输出\n限制位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;

                TimeLineEvents e = new TimeLineEvents();
                Vector3 pos = PlayerData.transform.position;
                for(int i = 1; i <= 5; i++)
                {
                    int j = i;
                    e.Add(0.1f*i, () => {
                        Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, pos+new Vector3(1*j,0,0),
                            Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 10), 1.2f, null, null);
                    });
                    e.Add(0.1f*i, () => {
                        Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, pos + new Vector3(-1 * j, 0, 0),
                            Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 10), 1.2f, null, null);
                    });
                }
                ApplyEvents(e);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 5f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : BulletBase
        {
            public override void Init()
            {
                LifeTime = 2f;

                DamageType = new Damage_Once();
            }
            public override Vector3 GetPosition()
            {
                return new Vector3(0, 0, 0);
            }
            public override float GetRotation()
            {
                return 90;
            }
        }
    }
    namespace Skill13
    {
        public class Skill : SkillBase
        {
            private Skill_Storable skill;
            private float storeTime = 4;
            private int cost = 40;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "护盾冲刺";
                Description = "生成生命值10%的护甲并冲刺，可储存4次，冷却25s，耗魔40";
                sprite = Tool.VarietyManager.GetSkillIcon(6, 1);
                TimeNeeded = 0.45f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime > 0.999f;
            }
            public override void UseSkill()
            {
                storeTime -= 1;
                if (isLocalPlayer) skill.SetAvailableTime(storeTime);
                ApplyMotion(new Motion() { RightDir = PlayerData.PlayerController.FaceRight ? 1 : -1 });
                PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Hudun, (int)(PlayerData.BaseAttributes.Shengming * 0.1f), 1);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 25;
                if (storeTime > 3) storeTime = 3;
                if (PlayerData.Mofa < cost)
                {
                    skill.SetAvailableTime(0);
                }
                else
                {
                    skill.SetAvailableTime(storeTime);
                }
            }
        }
        public class Motion : MotionBase
        {
            public int RightDir;
            public override void Init()
            {
                WorkTime = 0.5f;
            }
            public override Vector3 GetVelocity(Vector3 v)
            {
                return new Vector3(Mathf.Max(Mathf.Abs(v.x), 20) * RightDir, 0, 0);
            }
            public override Vector3 Exit(Vector3 v)
            {
                return new Vector3();
            }
        }
    }
    namespace Skill14
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 150;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "逃逸";
                Description = "恢复10%生命，速度提升50%，连跳次数+1，持续10秒，耗魔150，冷却60秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 10);
                TimeNeeded = 0.1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Shengming, 0.1f, 1);
                PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Jixing, 0.5f, 10);
                PlayerData.EffectController.AddEffectLocal(EffectController.Effects.Liantiao, 1, 10);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 60f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill15
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "撼地";
                Description = "震击地面，倍率6.4，耗魔100，冷却40秒";
                sprite = Tool.VarietyManager.GetSkillIcon(3, 2);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移\n爆发";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                ApplyMotion(new Motion());

                TimeLineEvents e = new TimeLineEvents();
                e.Add(0.25f, () =>
                {
                    Tool.BulletManager.ShootBullet(new Bullet1(), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 12), 6.4f, null, null);
                });
                ApplyEvents(e);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 40f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet1 : BulletBase
        {
            public override void Init()
            {
                LifeTime = 2f;

                DamageType = new Damage_Once();
            }
            public override Vector3 GetPosition()
            {
                return new Vector3(0, 0, 0);
            }
            public override float GetRotation()
            {
                return 90;
            }
        }
        public class Motion : MotionBase
        {
            public override void Init()
            {
                WorkTime = 0.2f;
            }
            public override Vector3 GetVelocity(Vector3 v)
            {
                return new Vector3(0,-100,0);
            }
        }
    }
    namespace Skill16
    {
        public class Skill : SkillBase
        {
            private Skill_Storable skill;
            private float storeTime = 2;
            private int cost = 100;
            private EffectCollection effect;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "撕裂伤口";
                Description = "发射追踪子弹攻击最近的玩家，命中时降低目标20%防御和100减伤，持续10s，可储存2次，耗魔120";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 5);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "追踪\n增伤";

                effect = new EffectCollection();
                effect.AddEffect(EffectController.Effects.Fangyu, -0.2f, 20);
                effect.AddEffect(EffectController.Effects.Jianshang, -100, 20);

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime > 0.999f;
            }
            public override void UseSkill()
            {
                var t=PlayerData.GetNearestEnemy(99999, false);
                Transform ts = null;
                if (t != null) ts = t.transform;
                storeTime -= 1;
                if (isLocalPlayer) skill.SetAvailableTime(storeTime);
                Tool.BulletManager.ShootBullet(new Bullet() { Target = ts }, PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 13), 1f, effect, null);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 5;
                if (storeTime > 2) storeTime = 2;
                if (PlayerData.Mofa < cost)
                {
                    skill.SetAvailableTime(0);
                }
                else
                {
                    skill.SetAvailableTime(storeTime);
                }
            }
        }
        public class Bullet : BulletBase
        {
            public Transform Target;
            private float Speed = 10;
            private bool On = true;
            private Vector3 Dir;
            public override void Init()
            {
                LifeTime = 10f;

                DamageType = new Damage_Once();
                
                MotionSpace = MoveSpace.World;
                if (Target == null)
                {
                    On = false;
                    Dir = Vector3.up;
                }
            }
            public override Vector3 GetPosition()
            {
                if(Target!=null)if ((Target.position - Bullet.transform.position).magnitude < 1) On = false;
                if (On)
                {
                    Dir = (Target.position - Bullet.transform.position).normalized;
                    return Dir * Speed * Time.deltaTime + Bullet.transform.position; 
                }
                else
                {
                    return Dir * Speed * Time.deltaTime + Bullet.transform.position;
                }
            }
            public override Vector3 GetPositionL()
            {
                return GetPosition();
            }
        }
    }*/
    
/*
 namespace Skill0
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 40;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "短距离冲刺";
                Description = "向前方冲刺一小段距离，耗魔40";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 0);
                TimeNeeded = 0.1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                ApplyMotion(new Motion() { faceRight = PlayerData.PlayerController.FaceRight });
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
        public class Motion : MotionBase
        {
            public bool faceRight;
            public override void Init()
            {
                WorkTime = 0.3f;
            }
            public override Vector3 GetVelocity(Vector3 v)
            {
                return new Vector3(faceRight ? 20 : -20, 1, 0);
            }
        }
    }
    namespace Skill1
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 0;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "生命燃烧";
                Description = "消耗生命，获得魔法，冷却10秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 3);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Shengming, -0.2f, 1);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Mofa, 0.2f, 1);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 10f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill2
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 0;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "魔力燃烧";
                Description = "消耗魔法恢复生命，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 2);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Shengming, 0.2f, 1);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Mofa, -0.2f, 1);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill3
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 40;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "临界加护";
                Description = "防御提升1000%，持续2s，耗魔40，冷却10秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 27);
                TimeNeeded = 0.1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Fangyu, 10f, 2);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 10f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill4
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "鼓舞";
                Description = "范围10格内己方角色攻击提升50%，持续10s，耗魔100，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 20);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增伤";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Gongji, 0.5f, 10);
                foreach(var i in PlayerData.GetPartnerInRange(10, false))
                {
                    i.GetEffectable().AddEffectLocal(Effectable.Effects.Gongji, 0.5f, 10);
                }
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill5
    {
        public class Skill : SkillBase
        {
            private Skill_Storable skill;
            private float storeTime = 6;
            private int cost = 40;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "虚幻之跃";
                Description = "原地飞跃并2s内提升闪避，可储存6次，耗魔40";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 12);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime > 0.999f;
            }
            public override void UseSkill()
            {
                storeTime -= 1;
                if (isLocalPlayer) skill.SetAvailableTime(storeTime);
                ApplyMotion(new Motion());
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Shanbi, 100, 2);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 10;
                if (storeTime > 6) storeTime = 6;
                if (PlayerData.Mofa < cost)
                {
                    skill.SetAvailableTime(0);
                }
                else
                {
                    skill.SetAvailableTime(storeTime);
                }
            }
        }
        public class Motion : MotionBase
        {
            public override void Init()
            {
                WorkTime = 0.5f;
            }
            public override Vector3 GetVelocity(Vector3 v)
            {
                return v;
            }
            public override Vector3 Entry(Vector3 v)
            {
                return new Vector3(v.x, Mathf.Max(v.y, 15), v.z);
            }
        }
    }
    namespace Skill6
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;

            private EffectCollection effects;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "破甲弹";
                Description = "向前射出5发子弹，倍率1.2，每颗子弹可降低5%防御，耗魔70，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 26);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增伤";

                effects =new EffectCollection();
                effects.AddEffect(Effectable.Effects.Fangyu, -0.05f, 15);

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                for(int i=0;i<5;i++)
                Tool.BulletManager.ShootBullet(new Template.Bullet.BulletDirect(i*10-20,10,5), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.2f, effects, null);

            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill7
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 20;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "突刺";
                Description = "向前快速冲刺并伤害沿途敌人，倍率3，耗魔20，冷却10秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 2);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                Tool.BulletManager.ShootBullet(new Template.Bullet.BulletSurround(0,0,0,0.4f,1), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 3f, null, null);
                ApplyMotion(new Motion() { faceRight=PlayerData.PlayerController.FaceRight});
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 10f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }

        public class Motion : MotionBase
        {
            public bool faceRight;
            public override void Init()
            {
                WorkTime = 0.3f;
            }
            public override Vector3 GetVelocity(Vector3 v)
            {
                return new Vector3(faceRight ? 20 : -20, 0, 0);
            }
        }
    }
    namespace Skill8
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 30;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "左脚踩右脚";
                Description = "向前上方飞跃小段距离，耗魔30";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 48);
                TimeNeeded = 0.1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                ApplyMotion(new Motion() { faceRight = PlayerData.PlayerController.FaceRight });
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
        public class Motion : MotionBase
        {
            public bool faceRight;
            public override void Init()
            {
                WorkTime = 0.3f;
            }
            public override Vector3 GetVelocity(Vector3 v)
            {
                return new Vector3(faceRight ? 10 : -10, 3, 0);
            }
        }
    }
    namespace Skill9
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "悬浮术";
                Description = "悬浮在空中4s，耗魔100，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(5, 2);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                ApplyMotion(new Motion() { player = PlayerData });
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
            public class Motion : MotionBase
            {
                private float y;
                public PlayerData player;
                public override void Init()
                {
                    WorkTime = 4f;
                    y=player.transform.position.y;
                }
                public override Vector3 GetVelocity(Vector3 v)
                {
                    if(player.transform.position.y<y)return new Vector3(v.x,1,0);
                    else return new Vector3(v.x, 0, 0);
                }
            }
        }
    }
    namespace Skill10
    {
        public class Skill : SkillBase
        {
            private Skill_Storable skill;
            private float MaxstoreTime = 5;
            private float storeTime = 3;
            private int cost = 0;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "狙击";
                Description = "射出一发子弹，自动索敌20格，倍率3，可储存5次,冷却3s";
                sprite = Tool.VarietyManager.GetSkillIcon(0, 13);
                TimeNeeded = 0.3f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "远程输出";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime > 0.999f;
            }
            public override void UseSkill()
            {
                storeTime -= 1;
                if (isLocalPlayer) skill.SetAvailableTime(storeTime);
                TimeLineEvents e=new TimeLineEvents();
                e.Add(0.2f, () =>
                {
                    var p = PlayerData.GetNearestEnemy(20, true);
                    Tool.BulletManager.ShootBullet(new Template.Bullet.BulletFT(PlayerData.transform.position, p ?
                        p.transform.position : PlayerData.PlayerController.FaceRight ?
                        PlayerData.transform.position + new Vector3(1, 0, 0) : PlayerData.transform.position + new Vector3(-1, 0, 0), 20, 10)
                        , PlayerData, PlayerData.transform.position,
                            Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 3f, null, null);
                });
                ApplyEvents(e);
                ApplyMotion(new Motion());
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 3;
                if (storeTime > MaxstoreTime) storeTime = MaxstoreTime;
                if (PlayerData.Mofa < cost)
                {
                    skill.SetAvailableTime(0);
                }
                else
                {
                    skill.SetAvailableTime(storeTime);
                }
            }
        }
        public class Motion : MotionBase
        {
            public override void Init()
            {
                WorkTime = 0.5f;
            }
            public override Vector3 GetVelocity(Vector3 v)
            {
                return new Vector3();
            }
        }
    }
    namespace Skill11
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 200;
            private float storeTime = 1;
            private EffectCollection e;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "弱点暴露";
                Description = "发射一圈子弹，倍率0.6，并在最近敌人处引发爆炸，10s内使其受伤增加35%，倍率2，耗魔200，冷却30秒";
                sprite = Tool.VarietyManager.GetSkillIcon(8, 8);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增伤";

                e = new EffectCollection();
                e.AddEffect(Effectable.Effects.FinalJianshang, -35, 10);

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                var p = PlayerData.GetNearestEnemy(20, false);
                Tool.BulletManager.ShootBullet(new Template.Bullet.BulletStatic(0.3f), PlayerData, p?p.transform.position:PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 2f, e, null);
                for(int i = 0; i < 8; i++)
                {
                    Tool.BulletManager.ShootBullet(new Template.Bullet.BulletDirect(i*45+22.5f,10,3f), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 13), 0.6f, null, null);
                }
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 30f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill12
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 100;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "大黑耗子";
                Description = "速度、闪避大幅提升，连跳+2，命中大幅降低，持续8s，耗魔100";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 47);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Shanbi, 300, 10);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Jixing, 1.2f, 10);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Mingzhong, -1f, 10);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Liantiao, 2, 10);
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill13
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 20;
            private float storeTime = 1;
            
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "余晖";
                Description = "生命低于20%可发动，生命快速流失，攻击提升60%，持续5s，" +
                    "3s后在自身处引发爆炸,倍率6，命中会恢复大量生命，耗魔20，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 40);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "治疗";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Shengming<PlayerData.BaseAttributes.Shengming*0.2f&&PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Shengming, -0.1f, 5);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Gongji, 0.6f, 5);
                TimeLineEvents e = new TimeLineEvents();
                e.Add(3f, () =>
                {
                    Tool.BulletManager.ShootBullet(new BulletStatic(0.3f), PlayerData, PlayerData.transform.position,
                            Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 8), 6f, null, new HitEvent());
                });
                ApplyEvents(e);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost&& PlayerData.Shengming < PlayerData.BaseAttributes.Shengming * 0.2f) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
        public class HitEvent : HitEventBase
        {
            public override void OnHit(Target attacker, Target defenser, BulletController bullet)
            {
                if(attacker is PlayerData)
                {
                    (attacker as PlayerData).Heal((int)(attacker.BaseAttributes.Shengming * 0.6f));
                }
            }
        }
    }
    namespace Skill14
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private float storeTime = 1;
            private int cost = 200;
            private int state = 0;
            private int r;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "再生秘法";
                Description = "使用后记录生命值，再次使用时和第一次使用时相比损失了多少生命就恢复双倍的损失量，耗魔200，冷却20s";
                sprite = Tool.VarietyManager.GetSkillIcon(3, 7);
                TimeNeeded = 0.1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "治疗";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return state==1||(PlayerData.Mofa>cost&&storeTime >= 0.999f);
            }
            public override void UseSkill()
            {
                if (state == 0)
                {
                    state = 1;
                    r = PlayerData.Shengming;
                    PlayerData.Mofa -= cost;
                }
                else if (state == 1)
                {
                    state = 0;
                    storeTime = 0;
                    if (PlayerData.Shengming < r)
                    {
                        int i = r - PlayerData.Shengming;
                        PlayerData.Heal(i *2);
                    }
                }
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa > cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill15
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 120;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "弱点指引";
                Description = "为我方角色提升30%的暴击和幸运加成，耗魔120，冷却30秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 1);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                var p = PlayerData.GetPartnerInRange(99999, false);
                p.Add(PlayerData);
                foreach(var i in p)
                {
                    i.GetEffectable().AddEffectLocal(Effectable.Effects.Baoji, 30, 10);
                    i.GetEffectable().AddEffectLocal(Effectable.Effects.Xingyun, 30, 10);
                }
                ApplyMotion(new Template.Motion.MotionStatic(new Vector3(),0.3f));
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 30f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill16
    {
        public class Skill : SkillBase
        {
            private Skill_Storable skill;
            private float storeTime = 2;
            private float MaxstoreTime = 2;
            private int cost = 60;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "空间传送";
                Description = "急速向前移动一段距离并提高闪避2s，可储存2次，耗魔60";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 23);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime > 0.999f;
            }
            public override void UseSkill()
            {
                storeTime -= 1;
                if (isLocalPlayer) skill.SetAvailableTime(storeTime);
                ApplyMotion(new Template.Motion.MotionHorizontal(PlayerData.FacingRight(), 100, 0.1f));
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Shanbi, 600, 2);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 5;
                if (storeTime > MaxstoreTime) storeTime = MaxstoreTime;
                if (PlayerData.Mofa < cost)
                {
                    skill.SetAvailableTime(0);
                }
                else
                {
                    skill.SetAvailableTime(storeTime);
                }
            }
        }
    }
    namespace Skill17
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 300;
            private float storeTime = 1;
            private TimeLineEvents e;
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "浮游炮";
                Description = "「模式限定」召唤两个浮游炮自动向最近的敌人攻击，持续30s，倍率0.2f，耗魔300，冷却60秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 5);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "远程输出";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                Tool.BulletManager.ShootBullet(new Template.Bullet.BulletSurround(0,0,2,30,1f), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 15), 0.2f, null, null);
                Tool.BulletManager.ShootBullet(new Template.Bullet.BulletSurround(180, 0, 2, 30, 1f), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 15), 0.2f, null, null);

                e = new TimeLineEvents();
                for (int i = 1; i <= 30; i++)
                {
                    e.Add(i, () =>
                    {
                        var p = PlayerData.GetNearestEnemy(99999, false);
                        if (!p) return;
                        Tool.BulletManager.ShootBullet(new Template.Bullet.BulletFT(PlayerData.transform.position + new Vector3(2, 0),
                            p.transform.position, 15, 5), PlayerData, PlayerData.transform.position + new Vector3(2, 0),
                            Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.2f, null, null);
                        Tool.BulletManager.ShootBullet(new Template.Bullet.BulletFT(PlayerData.transform.position - new Vector3(2, 0),
                            p.transform.position, 15, 5), PlayerData, PlayerData.transform.position - new Vector3(2, 0),
                            Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.2f, null, null);
                    });
                }
                ApplyEvents(e);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 60f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill18
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 300;
            private float storeTime = 1;
            private TimeLineEvents e;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "审判领域";
                Description = "「模式限定」持续伤害周围的敌人，治疗周围的友方并提高攻击，持续10s，耗魔300，冷却40秒";
                sprite = Tool.VarietyManager.GetSkillIcon(2, 14);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益\n治疗";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;

                e = new TimeLineEvents();
                for (int i = 0; i <= 10; i++)
                {
                    e.Add(i, () =>
                    {
                        Tool.BulletManager.ShootBullet(new Template.Bullet.BulletStatic(0.2f), PlayerData, 
                            PlayerData.transform.position,
                            Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 2f, null, null);
                        var p = PlayerData.GetPartnerInRange(5, false);
                        p.Add(PlayerData);
                        foreach (var j in p)
                        {
                            j.GetEffectable().AddEffectLocal(Effectable.Effects.Gongji, 400, 1.2f);
                            if (j is PlayerData)
                            {
                                (j as PlayerData).Heal((int)(PlayerData.BaseAttributes.Shengming * 0.05f));
                            }
                        }
                    });
                }
                ApplyEvents(e);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 40f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill19
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 150;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "螺旋升天";
                Description = "向上漂浮并不断向周围发射子弹，倍率0.1，耗魔150，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(2, 12);
                TimeNeeded = 5f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "输出\n大范围";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                
                TimeLineEvents e=new TimeLineEvents();
                for(int j = 1; j <= 10; j++)
                {
                    e.Add(j*0.5f, () =>
                    {
                        int k = j;
                        for (int i = 0; i < 10; i++)
                        {
                            Tool.BulletManager.ShootBullet(new Template.Bullet.BulletDirect(i * 36+5*k, 10, 10), PlayerData, PlayerData.transform.position,
                                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.1f, null, null);
                        }
                    });
                }
                ApplyEvents(e);
                ApplyMotion(new Template.Motion.MotionStatic(new Vector3(0, 3), 5));
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill20
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "超视距狙击";
                Description = "射出一发子弹，自动瞄准最近的前方的敌人，距离越远倍率越高，耗魔100，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 28);
                TimeNeeded = 0.5f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "远程\n爆发";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                var p = PlayerData.GetNearestEnemy(99999, true);
                if (!p) return;
                PlayerData.Mofa -= cost;
                storeTime = 0;
                ApplyMotion(new Template.Motion.MotionStatic(new Vector3(0, 0.2f), 0.5f));
                TimeLineEvents e=new TimeLineEvents();
                e.Add(0.4f, () =>
                {
                    float d = Tool.GetDistance(p.transform.position, PlayerData.transform.position);
                    Tool.BulletManager.ShootBullet(new BulletFT(PlayerData.transform.position, p.transform.position, 30, 7), PlayerData, PlayerData.transform.position,
                            Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1 + d / 5, null, null);
                });
                ApplyEvents(e);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill21
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 300;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "箭阵";
                Description = "「模式限定」发射多发飞弹射向最近的敌人，倍率1.6，耗魔350，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 1);
                TimeNeeded = 1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "输出\n大范围";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                for(int i = -5; i <= 5; i++)
                {
                    Tool.BulletManager.ShootBullet(new Bullet(PlayerData.transform.position+new Vector3(i*3,-5)), 
                        PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 15), 1.6f, null, null);
                }
                ApplyMotion(new Template.Motion.MotionStatic(new Vector3(0, 0.2f, 0),0.8f));
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : BulletBase
        {
            private Vector3 start;
            private Vector3 turn;
            public Bullet(Vector3 p)
            {
                turn = p;
            }
            public override void Init()
            {
                LifeTime = 5f;

                DamageType = new Damage_Time() { CoolDown=0.5f};

                MotionSpace = MoveSpace.World;

                start = Bullet.transform.position;
            }
            public override Vector3 GetPosition()
            {
                if (SpawnTime < 1)
                {
                    return start * (1 - SpawnTime) + turn * SpawnTime;
                }
                if (SpawnTime < 1.5f)
                {
                    return turn;
                }
                return new Vector3(turn.x, (SpawnTime-1.5f)*10, 0);
            }
            public override Vector3 GetPositionL()
            {
                return GetPosition();
            }
        }
    }
    namespace Skill22
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 450;
            private float storeTime = 1;
            private TimeLineEvents e;
            private EffectCollection ef;
            private List<Vector3> vectors = new List<Vector3>()
            {
                new Vector3(-1.7f,4),
                new Vector3(1.7f,4),
                new Vector3(-1.7f,-4),
                new Vector3(1.7f,-4),
                new Vector3(4,-1.7f),
                new Vector3(4,1.7f),
                new Vector3(-4,-1.7f),
                new Vector3(-4,1.7f)
            };
            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "超级浮游炮";
                Description = "「模式限定」创造多个浮游炮，向最近敌人发动攻击，命中时降低守护，倍率0.4，耗魔450，冷却10秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 39);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "远程输出";

                ef = new EffectCollection();
                ef.AddEffect(Effectable.Effects.Shouhu, -0.05f, 10);

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                foreach(var v in vectors)
                Tool.BulletManager.ShootBullet(new BulletStatic(10), PlayerData, PlayerData.transform.position+v,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.6f, null, null);

                e = new TimeLineEvents();
                for (int i = 1; i <= 15; i+=2)
                {
                    e.Add(i, () =>
                    {
                        var p = PlayerData.GetNearestEnemy(99999, false);
                        if (!p) return;
                        foreach(var v in vectors)
                        Tool.BulletManager.ShootBullet(new BulletFT(PlayerData.transform.position + v,
                            p.transform.position, 15, 5), PlayerData, PlayerData.transform.position + v,
                            Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.6f, ef, null);
                    });
                }
                ApplyEvents(e);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }*/
    
/*namespace Skill0
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "超级霸体";
                Description = "大幅提升抗击退和最终减伤，小幅提升速度，持续10s，耗魔100，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 37);
                TimeNeeded = 0.1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移\n霸体";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Kangjitui, 300, 10);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.FinalJianshang, 40, 10);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Jixing, 0.3f, 10);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill1
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 50;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "快速护盾";
                Description = "生成生命值5%的护盾，耗魔50，冷却10秒";
                sprite = Tool.VarietyManager.GetSkillIcon(0, 14);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Hudun, (int)(0.05f * PlayerData.BaseAttributes.Shengming), 1);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 10f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill2
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 400;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "世界树之叶";
                Description = "「模式限定」发射大量追踪子弹，倍率3，耗魔400";
                sprite = Tool.VarietyManager.GetSkillIcon(0, 17);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "远程输出\n爆发";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                var p = PlayerData.GetNearestEnemy(99999, false);
                if (!p) return;
                PlayerData.Mofa -= cost;
                for(int i = 0; i < 20; i++)
                {
                    Tool.BulletManager.ShootBullet(new Bullet(p.transform,i*18), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2.4f, null, null);
                }
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : BulletBase
        {
            private Transform Target;
            private float Degree;
            private bool hit = false;
            private float Speed=5;
            private float ds=60;
            public Bullet(Transform target,float degree)
            {
                Target=target;
                Degree=degree;
            }
            public override void Init()
            {
                LifeTime = 10f;

                DamageType = new Damage_Once();

                MotionSpace = MoveSpace.World;
            }
            public override Vector3 GetPosition()
            {
                if ((Bullet.transform.position - Target.position).magnitude < 1)
                {
                    hit = true;
                }
                if (hit)
                {
                    return Bullet.transform.position + Time.deltaTime*Speed*new Vector3(Mathf.Cos(Degree / 57.3f), Mathf.Sin(Degree / 57.3f));
                }
                else
                {
                    var vl = new Vector3(Mathf.Cos((Degree + 1) / 57.3f), Mathf.Sin((Degree + 1) / 57.3f));
                    var vr = new Vector3(Mathf.Cos((Degree - 1) / 57.3f), Mathf.Sin((Degree - 1) / 57.3f));
                    var v = Target.position - Bullet.transform.position;
                    if (Vector3.Dot(vl, v) > Vector3.Dot(vr, v))
                    {
                        Degree += Time.deltaTime * ds;
                    }
                    else
                    {
                        Degree-=Time.deltaTime * ds;
                    }
                    return Bullet.transform.position + Time.deltaTime * Speed *new Vector3(Mathf.Cos(Degree / 57.3f), Mathf.Sin(Degree / 57.3f));
                }
            }
            public override Vector3 GetPositionL()
            {
                return GetPosition();
            }
        }
    }
    namespace Skill3
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 300;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "哨箭";
                Description = "「模式限定」召唤一只箭不断攻击敌人，倍率2.4，持续10s，耗魔300";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 43);
                TimeNeeded = 0.1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "爆发";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                var p = PlayerData.GetNearestEnemy(99999, false);
                if (!p) return;
                PlayerData.Mofa -= cost;
                Tool.BulletManager.ShootBullet( new Bullet(p.transform,90) , PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 0), 2.4f, null, null);
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : BulletBase
        {
            private Transform Target;
            private float Degree;
            private float Speed = 20;
            private float ds = 540;
            public Bullet(Transform target, float degree)
            {
                Target = target;
                Degree = degree;
            }
            public override void Init()
            {
                LifeTime = 10f;

                DamageType = new Damage_Time() { CoolDown=0.3f};

                MotionSpace = MoveSpace.World;
            }
            public override Vector3 GetPosition()
            {
                if ((Bullet.transform.position - Target.position).magnitude < 1)
                {
                    return Bullet.transform.position + Time.deltaTime * Speed * new Vector3(Mathf.Cos(Degree / 57.3f), Mathf.Sin(Degree / 57.3f));
                }
                else
                {
                    var vl = new Vector3(Mathf.Cos((Degree + 1) / 57.3f), Mathf.Sin((Degree + 1) / 57.3f));
                    var vr = new Vector3(Mathf.Cos((Degree - 1) / 57.3f), Mathf.Sin((Degree - 1) / 57.3f));
                    var v = Target.position - Bullet.transform.position;
                    if (Vector3.Dot(vl, v) > Vector3.Dot(vr, v))
                    {
                        Degree += Time.deltaTime * ds;
                    }
                    else
                    {
                        Degree -= Time.deltaTime * ds;
                    }
                    return Bullet.transform.position + Time.deltaTime * Speed * new Vector3(Mathf.Cos(Degree / 57.3f), Mathf.Sin(Degree / 57.3f));
                }
            }
            public override Vector3 GetPositionL()
            {
                return GetPosition();
            }
        }
    }
    namespace Skill4
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 100;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "箭矢连发";
                Description = "连续向前方射出大量箭矢，倍率1.2f，耗魔100";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 42);
                TimeNeeded = 1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "爆发";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                TimeLineEvents e=new TimeLineEvents();
                for(float i = 0; i < 1; i += 0.1f)
                {
                    e.Add(i, () =>
                    {
                        Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, PlayerData.transform.position+Vector3.up*Random.Range(-0.25f,0.25f),
                            Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 0), 1.2f, null, null);
                    });
                }
                ApplyEvents(e);
                ApplyMotion(new Template.Motion.MotionStatic(new Vector3(), 1));
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : BulletBase
        {
            public override void Init()
            {
                LifeTime = 2f;

                DamageType = new Damage_Once();
            }
            public override Vector3 GetPosition()
            {
                return new Vector3(15 * SpawnTime, 0, 0);
            }
        }
    }
    namespace Skill5
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 15;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "狩猎时刻";
                Description = "向最近的敌人方向发射三发箭矢，倍率0.3/0.3/0.6，耗魔15";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 30);
                TimeNeeded = 0.3f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "远程输出";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                float d = 0;
                var p = PlayerData.GetNearestEnemy(20, true);
                if (!p) d = PlayerData.PlayerController.FaceRight?0:180;
                else
                {
                    var v=p.transform.position-PlayerData.transform.position;
                    d = Mathf.Atan2(v.y, v.x)*57.3f;
                }
                Tool.BulletManager.ShootBullet(new Bullet(d), PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 0), 1.2f, null, null);
                Tool.BulletManager.ShootBullet(new Bullet(d+10), PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 0), 0.6f, null, null);
                Tool.BulletManager.ShootBullet(new Bullet(d-10), PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 0), 0.6f, null, null);

                ApplyMotion(new Template.Motion.MotionStatic(new Vector3(),0.3f));
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : BulletBase
        {
            private Vector3 start;
            private Vector3 dir;
            public Bullet(float d)
            {
                dir = new Vector3(Mathf.Cos(d/57.3f), Mathf.Sin(d/57.3f), 0)*12;
            }
            public override void Init()
            {
                LifeTime = 2f;

                DamageType = new Damage_Once();

                MotionSpace = MoveSpace.World;

                start = Bullet.transform.position;
            }
            public override Vector3 GetPosition()
            {
                return start + SpawnTime * dir;
            }
            public override Vector3 GetPositionL()
            {
                return start + SpawnTime * dir;
            }
        }
    }
    namespace Skill6
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 0;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "泥沼之书";
                Description = "恢复50%的魔法，大幅降低移速和跳跃，持续5s，冷却30秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 25);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Mofa, 0.1f, 5);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Jixing, -0.6f, 5);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Tengkong, -0.4f, 5);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 30f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill7
    {
        public class Skill : SkillBase
        {
            private Skill_Storable skill;
            private float storeTime = 9;
            private float MaxstoreTime = 9;
            private int cost = 60;
            private EffectCollection e;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "移形换影";
                Description = "快速向前冲刺并释放一圈子弹，倍率0.2f，命中后降低5%闪避，持续10s，可储存9次，，冷却10s，耗魔60";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 44);
                TimeNeeded = 0.1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                e =new EffectCollection();
                e.AddEffect(Effectable.Effects.Shanbi, -0.05f, 10);

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime > 0.999f;
            }
            public override void UseSkill()
            {
                storeTime -= 1;
                if (isLocalPlayer) skill.SetAvailableTime(storeTime);
                for(int i=0;i<8;i++)
                Tool.BulletManager.ShootBullet(new Template.Bullet.BulletDirect(i*45,10,4), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.2f, e, null);
                ApplyMotion(new Template.Motion.MotionHorizontal(PlayerData.FacingRight(), 30, 0.2f));
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 10;
                if (storeTime > MaxstoreTime) storeTime = MaxstoreTime;
                if (PlayerData.Mofa < cost)
                {
                    skill.SetAvailableTime(0);
                }
                else
                {
                    skill.SetAvailableTime(storeTime);
                }
            }
        }
    }
    namespace Skill8
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 200;
            private float storeTime = 1;
            private TimeLineEvents e;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "陨石";
                Description = "召唤一颗巨大的子弹从天而降，一段时间后爆炸，造成高额伤害。爆炸无视敌方40%的防御且必定命中，耗魔200，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(0, 9);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "输出";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                Tool.BulletManager.ShootBullet(new Template.Bullet.BulletDirect(270,20,2.5f), PlayerData, PlayerData.transform.position+Vector3.up*50,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 13), 1.2f, null, null);
                e = new TimeLineEvents();
                Vector3 v = PlayerData.transform.position;
                e.Add(2.5f, () =>
                {
                    Tool.BulletManager.ShootBullet(new Bullet(), PlayerData, v,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 5f, null, null);
                });
                ApplyEvents(e);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : BulletBase
        {
            public override void Init()
            {
                LifeTime = 2f;

                DamageType = new Damage_Once();
            }
            public override Vector3 GetPosition()
            {
                return new Vector3(10 * SpawnTime, 0, 0);
            }
            public override int FigureDamage(MainAttributes attacker, MainAttributes defenser, float rate, out bool strike)
            {
                var dd=defenser.Clone();
                dd.Fangyu = (int)(dd.Fangyu * 0.6f);
                dd.Shanbi = 0;
                return base.FigureDamage(attacker, dd, rate, out strike);
            }
        }
    }
    namespace Skill9
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 50;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "再生之花";
                Description = "使用后提升生命恢复速度、攻击、命中，耗魔50，冷却30秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 20);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益\n治疗";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Shengming, 0.02f, 10);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Gongji, 0.2f, 10);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Mingzhong, 0.4f, 10);
                ApplyMotion(new Template.Motion.MotionStatic(new Vector3(), 0.5f));
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 30f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill10
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 30;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "猛击";
                Description = "只能在空中使用。向前方俯冲并伤害沿途敌人，倍率2，耗魔30";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 7);
                TimeNeeded = 0.5f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移\n输出";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost&&!PlayerData.PlayerController.isGrounded;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                Tool.BulletManager.ShootBullet(new Template.Bullet.BulletFollow(PlayerData.transform,0.25f), PlayerData, PlayerData.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 13), 2f, null, null);
                ApplyMotion(new Template.Motion.MotionHorizontal(PlayerData.FacingRight(), 20, 0.25f,-20));
            }
            public override void Update()
            {
                if (CanUse()) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill11
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 0;
            private EffectCollection e;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "大嘴巴子";
                Description = "攻击1格范围内前方最近的敌人，倍率1，降低防御并造成流血";
                sprite = Tool.VarietyManager.GetSkillIcon(0, 4);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "输出";

                e = new EffectCollection();
                e.AddEffect(Effectable.Effects.Shengming, -0.02f, 1);
                e.AddEffect(Effectable.Effects.Fangyu, -0.1f, 5);

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                var p = PlayerData.GetNearestEnemy(1.5f, true);
                if(!p)return false;
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                var p = PlayerData.GetNearestEnemy(3f,true);
                if (!p) return;
                PlayerData.Mofa -= cost;
                Tool.BulletManager.ShootBullet(new Template.Bullet.BulletStatic(0.3f), PlayerData, p.transform.position,
                    Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 5), 1f, e, null);
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill12
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 60;
            private float storeTime = 1;
            private TimeLineEvents e;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "连发铳";
                Description = "向前扇形范围连续发射多颗子弹，倍率0.3，耗魔60，冷却10秒";
                sprite = Tool.VarietyManager.GetSkillIcon(6, 14);
                TimeNeeded = 2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "输出";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                e = new TimeLineEvents();
                float offset = 0;
                for (float i = 0; i < 2; i += 0.1f)
                {
                    float f = i;
                    e.Add(i, () =>
                    {
                        Tool.BulletManager.ShootBullet(new Template.Bullet.BulletDirect(offset + 15*Mathf.Sin(f * 4), 20, 1), PlayerData, PlayerData.transform.position,
                                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.3f, null, null);
                    });
                }
                ApplyEvents(e);
                ApplyMotion(new Template.Motion.MotionStatic(new Vector3(), 2));
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 10f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill13
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 150;
            private float storeTime = 1;
            private static List<float> x = new List<float>()
            {
                -10,-8.6f,-7f,-5f,-2.2f,-0.4f,0.6f,1.7f,2.8f,4f,5.5f,6.9f,8f,9.2f,10
            };
            private static List<float> y = new List<float>()
            {
                2,0,4,6,5,-2,3,9,6,4,1,5,8,3,6
            };
            private EffectCollection e;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "冰锥";
                Description = "释放大量冰锥从天而降，倍率2，耗魔150，若释放时魔法高于300，则消耗300魔法并提升倍率至4且增大攻击范围并附加防御降低，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 11);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "爆发\n增伤";

                e =new EffectCollection();
                e.AddEffect(Effectable.Effects.Fangyu, -0.2f, 10);

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                storeTime = 0;
                if (PlayerData.Mofa >= 300)
                {
                    PlayerData.Mofa -= 300;
                    for (int i = 0; i < x.Count; i++)
                        Tool.BulletManager.ShootBullet(new Bullet(270,25,5), PlayerData, 
                            PlayerData.transform.position+new Vector3(x[i], y[i]+30),
                                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 11), 4f, e, null);
                }
                else
                {
                    PlayerData.Mofa -=cost;
                    for (int i = 0; i < x.Count; i++)
                        Tool.BulletManager.ShootBullet(new Template.Bullet.BulletDirect(270,20,5), PlayerData,
                            PlayerData.transform.position + new Vector3(x[i], y[i]+30),
                                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 2f, null, null);
                }
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : BulletBase
        {
            public float degree;
            public float speed;
            /// <summary>
            /// 需要设置degree speed
            /// </summary>
            public Bullet(float d, float s, float lifetime)
            {
                degree = d;
                speed = s;
                LifeTime = lifetime;
            }
            public override void Init()
            {
                //LifeTime = 2f;

                DamageType = new Damage_Once();
            }
            public override Vector3 GetPosition()
            {
                return new Vector3(Mathf.Cos(degree / 57.3f) * speed * SpawnTime, Mathf.Sin(degree / 57.3f) * speed * SpawnTime, 0);
            }
            public override float GetRotation()
            {
                return 90;
            }
            public override float GetRotationL()
            {
                return 90;
            }
        }
    }
    namespace Skill14
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 70;
            private float storeTime = 1;
            private int threshold;
            private TimeLineEvents e;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "灵力汇聚";
                Description = "生命大于50%可发动。缓慢上升并不断释放子弹，5s后一次性释放大量子弹，倍率1.6，" +
                    "若生命高于50%则追加12发自动索敌的子弹，并提升50%攻击，持续40s，耗魔200，冷却30秒";
                sprite = Tool.VarietyManager.GetSkillIcon(1, 0);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益\n输出";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);

                threshold = (int)(PlayerData.BaseAttributes.Shengming / 2f);
            }
            public override bool CanUse()
            {
                return PlayerData.Shengming>=threshold&&PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                ApplyMotion(new Template.Motion.MotionStatic(new Vector3(0, 1f, 0), 5));

                e = new TimeLineEvents();
                int count = 0;
                for(float i = 0; i < 4.9f; i += 0.5f)
                {
                    int t = count;
                    e.Add(i, () =>
                    {
                        for(int j=0;j<4;j++)
                            Tool.BulletManager.ShootBullet(new Template.Bullet.BulletDirect(j*90+t*9,10,3), PlayerData, PlayerData.transform.position,
                                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.6f, null, null);
                    });
                    count++;
                }
                e.Add(5, () =>
                {
                    if (PlayerData.Shengming >= threshold)
                    {
                        for (int j = 0; j < 20; j++)
                            Tool.BulletManager.ShootBullet(new Template.Bullet.BulletDirect(j * 18, 20, 3), PlayerData, PlayerData.transform.position,
                                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 1.6f, null, null);
                        PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Gongji, 0.5f, 40);
                    }
                });
                ApplyEvents(e);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Shengming >= threshold && PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill15
    {
        public class Skill : SkillBase
        {
            private Skill_Storable skill;
            private float storeTime = 3;
            private float MaxstoreTime = 3;
            private int cost = 40;
            private TimeLineEvents e;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "闪击";
                Description = "前方10格范围内有敌人时，迅速向敌人方向冲刺，并在敌人位置释放一次攻击，倍率1.2，可储存3次，耗魔40";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 3);
                TimeNeeded = 0.4f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.Storable) as Skill_Storable;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime > 0.999f;
            }
            public override void UseSkill()
            {
                var p = PlayerData.GetNearestEnemy(10, true);
                if (!p) return;
                storeTime -= 1;
                if (isLocalPlayer) skill.SetAvailableTime(storeTime);
                e = new TimeLineEvents();
                e.Add(0.2f, () =>
                {
                    Tool.BulletManager.ShootBullet(new Template.Bullet.BulletStatic(0.2f), PlayerData, p.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 4), 1.2f, null, null);
                });
                ApplyEvents(e);
                var v = p.transform.position - PlayerData.transform.position;
                ApplyMotion(new Template.Motion.MotionStatic(v*3,0.4f));
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 5;
                if (storeTime > MaxstoreTime) storeTime = MaxstoreTime;
                if (PlayerData.Mofa < cost)
                {
                    skill.SetAvailableTime(0);
                }
                else
                {
                    skill.SetAvailableTime(storeTime);
                }
            }
        }
    }
    namespace Skill16
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 250;
            private float storeTime = 1;
            private EffectCollection e;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "尸骸化";
                Description = "「模式限定」获得100减伤，移速增加30%，持续60s，生命变为上限的30%，最终减伤提高50%，持续10s，释放出一圈旋转的子弹，倍率0.2，" +
                    "命中后会造成攻击降低2%，持续10s，耗魔300，冷却60秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 34);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "位移";

                e = new EffectCollection();
                e.AddEffect(Effectable.Effects.Gongji, -0.02f, 10);

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.Shengming = (int)(PlayerData.BaseAttributes.Shengming * 0.3f);
                for(int i=0;i<36;i++)
                    Tool.BulletManager.ShootBullet(new BulletRotate(i*10,10,90,1), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.2f, e, null);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Jianshang, 100, 60);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Jixing, 0.3f, 60);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.FinalJianshang, 50, 10);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
        public class BulletRotate : BulletBase
        {
            private float degree;
            private float degreespeed;
            private float speed;
            /// <summary>
            /// 需要设置degree speed
            /// </summary>
            public BulletRotate(float d, float s, float ds, float lifetime)
            {
                degree = d;
                degreespeed = ds;
                speed = s;
                LifeTime = lifetime;
            }
            public override void Init()
            {
                //LifeTime = 2f;

                DamageType = new Damage_Once();
            }
            public override Vector3 GetPosition()
            {
                degree += degreespeed * Time.deltaTime;
                return new Vector3(Mathf.Cos(degree / 57.3f) * speed * SpawnTime, Mathf.Sin(degree / 57.3f) * speed * SpawnTime, 0);
            }
        }
    }
    namespace Skill17
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;
            private List<float> degrees = new List<float>()
            {
                19,387,983,193,972,173,328,937,236,100,181,118,102,283,237,938,659,103,572,492
            };

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "裁断之剑";
                Description = "5s内，提升20%命中和暴击，对周围10格内的敌人不断释放斩击，倍率0.6，耗魔200，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 36);
                TimeNeeded = 0.1f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "输出";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                TimeLineEvents e = new TimeLineEvents();
                for(int i = 0; i < 20; i++)
                {
                    int t = i;
                    e.Add(0.25f * i, () =>
                    {
                        var j = PlayerData.GetEnemyInRange(10, false);
                        foreach(var p in j)
                        {
                            float d = degrees[t];
                            Vector3 v = new Vector3(Mathf.Cos(d), Mathf.Sin(d))*2.5f;
                            Tool.BulletManager.ShootBullet(new Template.Bullet.BulletFT(p.transform.position+v,p.transform.position-v, 20, 0.25f), PlayerData, PlayerData.transform.position,
                                Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.6f, null, null);
                        }
                    });
                }
                ApplyEvents(e);

                PlayerData.GetEffectable().AddEffectLocal(Effectable.Effects.Mingzhong, 20, 5);
                PlayerData.GetEffectable().AddEffectLocal(Effectable.Effects.Baoji, 20, 5);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill18
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "灵魂之握";
                Description = "消耗100/200/300魔法，影响10/20/30格范围内的敌人，使其5s内无法移动(对boss无效)，造成1.2/3.6/6倍率的伤害，" +
                    "最终减伤降低10/20/30，持续20s，每影响到一个敌人，自身攻击提升10%，移速提升5%，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 22);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益\n输出\n控制";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                storeTime = 0;
                float r = 0;
                float rate = 0;
                float jianshang = 0;
                if (PlayerData.Mofa >= 300)
                {
                    PlayerData.Mofa -= 300;
                    r = 30;
                    rate = 6;
                    jianshang = 30;
                }
                else if (PlayerData.Mofa >= 200)
                {
                    PlayerData.Mofa -= 200;
                    r = 20;
                    rate = 3.6f;
                    jianshang = 20;
                }
                else
                {
                    PlayerData.Mofa -= 100;
                    r = 10;
                    rate = 1.2f;
                    jianshang = 10;
                }
                EffectCollection e=new EffectCollection();
                e.AddEffect(Effectable.Effects.FinalJianshang, -jianshang, 20);
                int c = 0;
                foreach(var p in PlayerData.GetEnemyInRange(r, false))
                {
                    c += 1;
                    Tool.BulletManager.ShootBullet(new Template.Bullet.BulletStatic(0.2f), PlayerData, p.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), rate, e, null);
                    ApplyMotionTo(new Template.Motion.MotionStatic(new Vector3(), 5f),p);
                }
                if (c > 0)
                {
                    PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Gongji, 0.1f * c, 20);
                    PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Jixing, 0.05f * c, 20);
                }
                
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill19
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "生命精华";
                Description = "生命回复20%，攻击提升20%，移速降低40%，防御降低40%，持续20s，耗魔100，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 47);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Shengming, 0.01f, 20);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Gongji, 0.2f, 20);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Jixing, -0.4f, 20);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Fangyu, -0.4f, 20);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill20
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "魔力精华";
                Description = "魔法回复40%，暴击提升20%，防御降低40%，腾空降低20%，持续20s，耗魔100，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 48);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Mofa, 0.02f, 20);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Baoji, 0.2f, 20);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Fangyu, -0.4f, 20);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Tengkong, -0.2f, 20);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill21
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 100;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "治愈精华";
                Description = "生命回复20%，魔法回复20%，攻击降低40%，若生命低于50%，最终减伤提升40%，持续20s，耗魔100，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 49);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Shengming, 0.01f, 20);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Mofa, 0.01f, 20);
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Gongji, -0.4f, 20);
                if(PlayerData.Shengming<PlayerData.BaseAttributes.Shengming/2f)PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Tengkong, 40, 20);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill22
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 200;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "终焉轮舞";
                Description = "释放多发子弹，在自身周围来回震荡，倍率0.6，耗魔200";
                sprite = Tool.VarietyManager.GetSkillIcon(5, 0);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "近身爆发";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                for(int i=0;i<10;i++)
                    Tool.BulletManager.ShootBullet(new Bullet(i*36), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.6f, null, null);
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : BulletBase
        {
            private float d;
            public Bullet(float degree)
            {
                d = degree;
            }
            public override void Init()
            {
                LifeTime = 10f;

                DamageType = new Damage_Time() { CoolDown=0.5f};

                MotionSpace = MoveSpace.Player;
            }
            public override Vector3 GetPosition()
            {
                float r = Mathf.Clamp(Mathf.Sin(SpawnTime)*10,-5,5);
                d += 90 * Time.deltaTime;
                return new Vector3(Mathf.Cos(d)*r, Mathf.Sin(d)*r, 0);
            }
        }
    }*/
    
/*
 namespace Skill0
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 300;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "持久战-生命";
                Description = "「模式限定」永久提升生命上限2000点，耗魔300";
                sprite = Tool.VarietyManager.GetSkillIcon(2, 0);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                PlayerData.HealthBoost(2000);
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill1
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 300;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "持久战-魔力";
                Description = "「模式限定」永久提升魔力上限20点，耗魔300";
                sprite = Tool.VarietyManager.GetSkillIcon(8, 7);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                PlayerData.MagicBoost(20);
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill2
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 300;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "持久战-攻击";
                Description = "「模式限定」永久提升攻击100点，耗魔300";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 14);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Gongji, 100, 9999999);
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill3
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 300;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "持久战-命中";
                Description = "「模式限定」永久提升命中20点，耗魔300";
                sprite = Tool.VarietyManager.GetSkillIcon(6, 6);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Mingzhong, 20, 999999);
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill4
    {
        public class Skill : SkillBase
        {
            private Skill_NonCD skill;
            private int cost = 150;


            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "星落";
                Description = "召唤三颗流行砸向前方，耗魔150";
                sprite = Tool.VarietyManager.GetSkillIcon(7, 32);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "输出";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.NonCD) as Skill_NonCD;
                skill.Init(key, sprite);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                int x = PlayerData.PlayerController.FaceRight ? 1 : -1;
                Vector3 p = PlayerData.transform.position;
                float offset = p.y;
                TimeLineEvents e=new TimeLineEvents();
                e.Add(0, () =>
                {
                    Tool.BulletManager.ShootBullet(new Template.Bullet.BulletFT(new Vector3(p.x+x*-5,17+offset),new Vector3(p.x + x *5,offset),20,1), PlayerData, p,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.8f, null, null);
                });
                e.Add(0.5f, () =>
                {
                    Tool.BulletManager.ShootBullet(new Template.Bullet.BulletFT(new Vector3(p.x+0, 17+offset), new Vector3(p.x + x * 10, offset), 20, 1), PlayerData, p+new Vector3(x*5,0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.8f, null, null);
                });
                e.Add(1f, () =>
                {
                    Tool.BulletManager.ShootBullet(new Template.Bullet.BulletFT(new Vector3(p.x+x*5, 17+offset), new Vector3(p.x + x * 15, offset), 20, 1), PlayerData, p + new Vector3(x*10, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.8f, null, null);
                });

                e.Add(1, () =>
                {
                    Tool.BulletManager.ShootBullet(new Template.Bullet.BulletStatic(0.3f), PlayerData, p +new Vector3(x*5, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 17), 4f, null, null);
                });
                e.Add(1.5f, () =>
                {
                    Tool.BulletManager.ShootBullet(new Template.Bullet.BulletStatic(0.3f), PlayerData, p + new Vector3(x * 10, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 17), 4f, null, null);
                });
                e.Add(2, () =>
                {
                    Tool.BulletManager.ShootBullet(new Template.Bullet.BulletStatic(0.3f), PlayerData, p + new Vector3(x * 15, 0),
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 17), 4f, null, null);
                });
                ApplyEvents(e);
            }
            public override void Update()
            {
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(1);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill5
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 250;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "生命萃取";
                Description = "「模式限定」每秒损失5%生命值，持续5s，5s后回复50%生命值，耗魔250，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(8, 19);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Shengming, -0.05f, 5);
                TimeLineEvents e=new TimeLineEvents();
                e.Add(5, () =>
                {
                    PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Shengming, 0.5f, 1);
                });
                ApplyEvents(e);
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }
    namespace Skill6
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 70;
            private float storeTime = 1;
            private int threshold;
            private bool On;
            private float timecount = 0;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "护盾凝结";
                Description = "静止且护盾低于生命值上限30%时可释放，每秒获得生命值上限1%的护盾，移动或护盾超过生命值上限30%后失效，倍率0.4，耗魔70，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(8, 14);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增益";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);

                threshold = (int)(PlayerData.BaseAttributes.Shengming * 0.3f);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f&&PlayerData.PlayerController.GetVelocity().magnitude<0.1f&&PlayerData.Hudun<threshold&&!On;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                On = true;
                timecount = 0;
            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);

                if (On)
                {
                    if(PlayerData.PlayerController.GetVelocity().magnitude < 0.1f && PlayerData.Hudun < threshold)
                    {
                        timecount += Time.deltaTime;
                        if (timecount > 1)
                        {
                            timecount -= 1;
                            PlayerData.EffectController.AddEffectLocal(Effectable.Effects.Hudun, (int)(0.01f * PlayerData.BaseAttributes.Shengming), 1);
                        }
                    }
                    else
                    {
                        On = false;
                    }
                }
            }
        }
    }
    namespace Skill7
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 0;
            private float storeTime = 1;
            private EffectCollection e;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "引导者";
                Description = "在自身上方召唤1颗子弹，会向自身方向移动，存在20s，接触到自身后停止追踪，倍率4，不会被闪避且命中后降低目标防御30%，降低闪避30%，持续20s，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(6, 9);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "增伤";

                e = new EffectCollection();
                e.AddEffect(Effectable.Effects.Shanbi, -0.3f, 20);
                e.AddEffect(Effectable.Effects.Fangyu, -0.3f, 20);

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                Tool.BulletManager.ShootBullet(new Bullet(PlayerData.transform,-90,45,5,20), PlayerData, PlayerData.transform.position+Vector3.up*10,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1),4f, e, null);

            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
        public class Bullet : Template.Bullet.BulletTrack
        {
            public Bullet(Transform target, float degree, float degreespeed, float speed, float liftime) : base(target, degree, degreespeed, speed, liftime)
            {
            }
            public override int FigureDamage(MainAttributes attacker, MainAttributes defenser, float rate, out bool strike)
            {
                var a=attacker.Clone();
                a.Mingzhong = 9999999;
                return base.FigureDamage(attacker, defenser, rate, out strike);
            }
        }
    }
    namespace Skill8
    {
        public class Skill : SkillBase
        {
            private Skill_CD skill;
            private int cost = 300;
            private float storeTime = 1;

            public override void Init(PlayerData data, KeyCode key, bool isLocalPlayer)
            {
                PlayerData = data;

                Name = "护体罡气";
                Description = "「模式限定」发射大量子弹围绕自身，倍率0.4，耗魔300，冷却20秒";
                sprite = Tool.VarietyManager.GetSkillIcon(9, 8);
                TimeNeeded = 0.2f;
                this.isLocalPlayer = isLocalPlayer;

                Tag = "近身爆发";

                if (data == null) return;
                if (!isLocalPlayer) return;
                skill = Tool.Instance.CreateSkillColumn(PlayModePage.SkillColumnType.CD) as Skill_CD;
                skill.Init(key, sprite);
                skill.SetAvailableTime(storeTime);
            }
            public override bool CanUse()
            {
                return PlayerData.Mofa >= cost && storeTime >= 0.999f;
            }
            public override void UseSkill()
            {
                PlayerData.Mofa -= cost;
                storeTime = 0;
                for(int i=0;i<20;i++)
                Tool.BulletManager.ShootBullet(new Template.Bullet.BulletTrackRD(PlayerData.transform,0.5f,i*18,540*Random.Range(0.9f,1.1f),20*Random.Range(0.9f,1.1f),20), PlayerData, PlayerData.transform.position,
                        Tool.GlobalInf.BulletObject.GetBullet(PlayerData.Camp, 1), 0.4f, null, null);

            }
            public override void Update()
            {
                storeTime += Time.deltaTime / 20f;
                if (storeTime > 1) storeTime = 1;
                if (PlayerData.Mofa >= cost) skill.SetAvailableTime(storeTime);
                else skill.SetAvailableTime(0);
            }
        }
    }*/